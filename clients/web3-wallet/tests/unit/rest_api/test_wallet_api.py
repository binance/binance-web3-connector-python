"""
Binance Wallet REST API

Cross-chain wallet, market, trading, and transaction APIs for the OnchainOS platform.
The version of the OpenAPI document: 1.0.0
Contact: wallet-api@binance.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_web3_wallet.rest_api.api import WalletApi
from binance_sdk_web3_wallet.rest_api.models import GetAllTokenBalancesByAddressResponse
from binance_sdk_web3_wallet.rest_api.models import GetTokenBalancesByAddressResponse
from binance_sdk_web3_wallet.rest_api.models import GetTotalValueByAddressResponse
from binance_sdk_web3_wallet.rest_api.models import GetWalletSupportedChainsResponse
from binance_sdk_web3_wallet.rest_api.models import (
    GetTokenBalancesByAddressRequestTokenContractAddressesInner,
)
from binance_sdk_web3_wallet.rest_api.models import (
    GetTokenBalancesByAddressExcludeRiskTokenEnum,
)
from binance_sdk_web3_wallet.rest_api.models import GetTotalValueByAddressAssetTypeEnum


class TestWalletApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = WalletApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.web3_signature")
    def test_get_all_token_balances_by_address_success(self, mock_get_signature):
        """Test get_all_token_balances_by_address() successfully with required parameters only."""

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "page": 1,
                    "pageSize": 20,
                    "tokenAssets": [
                        {
                            "binanceChainId": "1",
                            "tokenContractAddress": "0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                            "address": "0xed0c6079229e2d407672a117c22b62064f4a4312",
                            "symbol": "WBTC",
                            "balance": "329.10055205",
                            "rawBalance": "32910055205",
                            "tokenPrice": "98847.8",
                            "isRiskToken": False,
                        }
                    ],
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_all_token_balances_by_address()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert (
            "/api/v1/dex/balance/all-token-balances-by-address" in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetAllTokenBalancesByAddressResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetAllTokenBalancesByAddressResponse, "from_dict")
        ):
            expected = GetAllTokenBalancesByAddressResponse.from_dict(expected_response)
        else:
            expected = GetAllTokenBalancesByAddressResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_all_token_balances_by_address_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_all_token_balances_by_address() successfully with optional parameters."""

        params = {
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
            "address": "0xed0c6079229e2d407672a117c22b62064f4a4312",
            "chains": "1,56",
            "exclude_risk_token": True,
            "page": 1,
            "page_size": 20,
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "page": 1,
                    "pageSize": 20,
                    "tokenAssets": [
                        {
                            "binanceChainId": "1",
                            "tokenContractAddress": "0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                            "address": "0xed0c6079229e2d407672a117c22b62064f4a4312",
                            "symbol": "WBTC",
                            "balance": "329.10055205",
                            "rawBalance": "32910055205",
                            "tokenPrice": "98847.8",
                            "isRiskToken": False,
                        }
                    ],
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_all_token_balances_by_address(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert (
            "/api/v1/dex/balance/all-token-balances-by-address" in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetAllTokenBalancesByAddressResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetAllTokenBalancesByAddressResponse, "from_dict")
        ):
            expected = GetAllTokenBalancesByAddressResponse.from_dict(expected_response)
        else:
            expected = GetAllTokenBalancesByAddressResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_all_token_balances_by_address_server_error(self):
        """Test that get_all_token_balances_by_address() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_all_token_balances_by_address = MagicMock(
            side_effect=mock_error
        )

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_all_token_balances_by_address()

    @patch("binance_common.utils.web3_signature")
    def test_get_token_balances_by_address_success(self, mock_get_signature):
        """Test get_token_balances_by_address() successfully with required parameters only."""

        params = {
            "address": "address_example",
            "token_contract_addresses": [
                GetTokenBalancesByAddressRequestTokenContractAddressesInner(
                    binance_chain_id="1",
                    token_contract_address="0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                )
            ],
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "tokenAssets": [
                        {
                            "binanceChainId": "1",
                            "tokenContractAddress": "0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                            "address": "0x50c476a139aab23fdaf9bca12614cdd54a4244e3",
                            "symbol": "WBTC",
                            "balance": "329.10055205",
                            "rawBalance": "32910055205",
                            "tokenPrice": "98847.8",
                            "isRiskToken": False,
                        }
                    ]
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_token_balances_by_address(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/balance/token-balances-by-address" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert normalized["address"] == "address_example"
        assert "tokenContractAddresses" in normalized

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetTokenBalancesByAddressResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetTokenBalancesByAddressResponse, "from_dict")
        ):
            expected = GetTokenBalancesByAddressResponse.from_dict(expected_response)
        else:
            expected = GetTokenBalancesByAddressResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_token_balances_by_address_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_token_balances_by_address() successfully with optional parameters."""

        params = {
            "address": "address_example",
            "token_contract_addresses": [
                GetTokenBalancesByAddressRequestTokenContractAddressesInner(
                    binance_chain_id="1",
                    token_contract_address="0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                )
            ],
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
            "exclude_risk_token": GetTokenBalancesByAddressExcludeRiskTokenEnum[
                "EXCLUDE_RISK_TOKEN_0"
            ].value,
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "tokenAssets": [
                        {
                            "binanceChainId": "1",
                            "tokenContractAddress": "0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                            "address": "0x50c476a139aab23fdaf9bca12614cdd54a4244e3",
                            "symbol": "WBTC",
                            "balance": "329.10055205",
                            "rawBalance": "32910055205",
                            "tokenPrice": "98847.8",
                            "isRiskToken": False,
                        }
                    ]
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_token_balances_by_address(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/balance/token-balances-by-address" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetTokenBalancesByAddressResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetTokenBalancesByAddressResponse, "from_dict")
        ):
            expected = GetTokenBalancesByAddressResponse.from_dict(expected_response)
        else:
            expected = GetTokenBalancesByAddressResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_token_balances_by_address_missing_required_param_address(self):
        """Test that get_token_balances_by_address() raises RequiredError when 'address' is missing."""
        params = {
            "address": "address_example",
            "token_contract_addresses": [
                GetTokenBalancesByAddressRequestTokenContractAddressesInner(
                    binance_chain_id="1",
                    token_contract_address="0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                )
            ],
        }
        params["address"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'address'"):
            self.client.get_token_balances_by_address(**params)

    def test_get_token_balances_by_address_missing_required_param_token_contract_addresses(
        self,
    ):
        """Test that get_token_balances_by_address() raises RequiredError when 'token_contract_addresses' is missing."""
        params = {
            "address": "address_example",
            "token_contract_addresses": [
                GetTokenBalancesByAddressRequestTokenContractAddressesInner(
                    binance_chain_id="1",
                    token_contract_address="0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                )
            ],
        }
        params["token_contract_addresses"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'token_contract_addresses'"
        ):
            self.client.get_token_balances_by_address(**params)

    def test_get_token_balances_by_address_server_error(self):
        """Test that get_token_balances_by_address() raises an error when the server returns an error."""

        params = {
            "address": "address_example",
            "token_contract_addresses": [
                GetTokenBalancesByAddressRequestTokenContractAddressesInner(
                    binance_chain_id="1",
                    token_contract_address="0x2260fac5e5542a773aa44fbcfedf7c193bc2c599",
                )
            ],
        }

        mock_error = Exception("ResponseError")
        self.client.get_token_balances_by_address = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_token_balances_by_address(**params)

    @patch("binance_common.utils.web3_signature")
    def test_get_total_value_by_address_success(self, mock_get_signature):
        """Test get_total_value_by_address() successfully with required parameters only."""

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {"totalValue": "12500.50"},
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_total_value_by_address()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/balance/total-value-by-address" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetTotalValueByAddressResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(GetTotalValueByAddressResponse, "from_dict")
        ):
            expected = GetTotalValueByAddressResponse.from_dict(expected_response)
        else:
            expected = GetTotalValueByAddressResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_total_value_by_address_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_total_value_by_address() successfully with optional parameters."""

        params = {
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
            "address": "0x50c476a139aab23fdaf9bca12614cdd54a4244e3",
            "chains": "1,56",
            "asset_type": GetTotalValueByAddressAssetTypeEnum["ASSET_TYPE_0"].value,
            "exclude_risk_token": True,
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {"totalValue": "12500.50"},
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_total_value_by_address(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/balance/total-value-by-address" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetTotalValueByAddressResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(GetTotalValueByAddressResponse, "from_dict")
        ):
            expected = GetTotalValueByAddressResponse.from_dict(expected_response)
        else:
            expected = GetTotalValueByAddressResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_total_value_by_address_server_error(self):
        """Test that get_total_value_by_address() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_total_value_by_address = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_total_value_by_address()

    @patch("binance_common.utils.web3_signature")
    def test_get_wallet_supported_chains_success(self, mock_get_signature):
        """Test get_wallet_supported_chains() successfully with required parameters only."""

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "binanceChainId": "1",
                    "name": "Ethereum",
                    "shortName": "ETH",
                    "logoUrl": "https://static.onchainos.com/chains/ethereum.png",
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_wallet_supported_chains()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/balance/supported/chain" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetWalletSupportedChainsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetWalletSupportedChainsResponse, "from_dict")
        ):
            expected = GetWalletSupportedChainsResponse.from_dict(expected_response)
        else:
            expected = GetWalletSupportedChainsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_wallet_supported_chains_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_wallet_supported_chains() successfully with optional parameters."""

        params = {
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
            "binance_chain_id": "1",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "binanceChainId": "1",
                    "name": "Ethereum",
                    "shortName": "ETH",
                    "logoUrl": "https://static.onchainos.com/chains/ethereum.png",
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_wallet_supported_chains(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/balance/supported/chain" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetWalletSupportedChainsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetWalletSupportedChainsResponse, "from_dict")
        ):
            expected = GetWalletSupportedChainsResponse.from_dict(expected_response)
        else:
            expected = GetWalletSupportedChainsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_wallet_supported_chains_server_error(self):
        """Test that get_wallet_supported_chains() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_wallet_supported_chains = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_wallet_supported_chains()
