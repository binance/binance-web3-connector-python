"""
Binance Web3 Wallet REST API

Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch

from binance_common.configuration import ConfigurationRestAPI
from binance_common.utils import is_one_of_model

from binance_sdk_web3_wallet.rest_api.api import WebSocketApi
from binance_sdk_web3_wallet.rest_api.models import GetWebSocketAuthTokenResponse


class TestWebSocketApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = WebSocketApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.web3_signature")
    def test_get_web_socket_auth_token_success(self, mock_get_signature):
        """Test get_web_socket_auth_token() successfully with required parameters only."""

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {
                "token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpc3MiOiJvbmNoYWlub3MtbWFya2V0IiwiYXVkIjoidzN3LXdzcCIsImFwayI6Inczd0BvbmNoYWlub3NAcHViIiwiaWF0IjoxNzg4MjYxMDY1LCJleHAiOjE3ODg4NjU4NjV9.feuZY7YDqaPrGTDVtfOwyH4Goq9rewLbokflraq1IrY"
            },
            "timestamp": 1788261065615,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_web_socket_auth_token()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/wss/auth/token" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetWebSocketAuthTokenResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetWebSocketAuthTokenResponse, "from_dict"):
            expected = GetWebSocketAuthTokenResponse.from_dict(expected_response)
        else:
            expected = GetWebSocketAuthTokenResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_web_socket_auth_token_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_web_socket_auth_token() successfully with optional parameters."""

        params = {"recv_window": 5000, "nonce": "unique-nonce-string"}

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {
                "token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpc3MiOiJvbmNoYWlub3MtbWFya2V0IiwiYXVkIjoidzN3LXdzcCIsImFwayI6Inczd0BvbmNoYWlub3NAcHViIiwiaWF0IjoxNzg4MjYxMDY1LCJleHAiOjE3ODg4NjU4NjV9.feuZY7YDqaPrGTDVtfOwyH4Goq9rewLbokflraq1IrY"
            },
            "timestamp": 1788261065615,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_web_socket_auth_token(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/wss/auth/token" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetWebSocketAuthTokenResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetWebSocketAuthTokenResponse, "from_dict"):
            expected = GetWebSocketAuthTokenResponse.from_dict(expected_response)
        else:
            expected = GetWebSocketAuthTokenResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_web_socket_auth_token_server_error(self):
        """Test that get_web_socket_auth_token() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_web_socket_auth_token = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_web_socket_auth_token()
