"""
Binance Web3 Wallet REST API

Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import requests
from typing import Optional, List, TypeVar, Union
from binance_common.configuration import ConfigurationRestAPI
from binance_common.models import ApiResponse
from binance_common.signature import Signers
from binance_common.utils import send_request
from .api.address_portfolio_api import AddressPortfolioApi
from .api.b402_payments_api import B402PaymentsApi
from .api.defi_data_api import DefiDataApi
from .api.defi_transaction_api import DefiTransactionApi
from .api.general_data_api import GeneralDataApi
from .api.rwa_data_api import RWADataApi
from .api.trading_api import TradingApi
from .api.transaction_api import TransactionApi
from .api.wallet_api import WalletApi

from .models import GetAddressPnLForSpecificTokenResponse
from .models import GetAddressPortfolioOverviewResponse
from .models import GetAddressRecentPnLResponse
from .models import GetDexTradeHistoryResponse
from .models import GetLeaderboardResponse
from .models import GetPortfolioSupportedChainsResponse
from .models import GetTrackedTradesResponse
from .models import GetAddressPortfolioOverviewTimeFrameEnum
from .models import GetDexTradeHistoryTypeEnum
from .models import GetLeaderboardTimeFrameEnum
from .models import GetLeaderboardSortByEnum
from .models import GetLeaderboardWalletTypeEnum
from .models import GetTrackedTradesTrackerTypeEnum
from .models import GetB402SupportedConfigurationsV1Response
from .models import GetB402SupportedConfigurationsV2Response
from .models import SettleB402PaymentV1Response
from .models import SettleB402PaymentV2Response
from .models import VerifyB402PaymentV1Response
from .models import VerifyB402PaymentV2Response
from .models import B402SettleEnvelopeV1Body
from .models import B402SettleEnvelopeV2Body
from .models import VerifyB402PaymentV1RequestBody
from .models import VerifyB402PaymentV2RequestBody
from .models import GetDeFiPositionsResponse
from .models import GetInvestmentDetailResponse
from .models import GetProtocolDetailResponse
from .models import ListDeFiInvestmentsResponse
from .models import ListDeFiProtocolsResponse
from .models import ListDeFiInvestmentsInvestTypeEnum
from .models import ListDeFiInvestmentsSortDirectionEnum
from .models import ListDeFiProtocolsInvestTypeEnum
from .models import ListDeFiProtocolsSortDirectionEnum
from .models import BuildDeFiClaimTransactionResponse
from .models import BuildDeFiDepositTransactionResponse
from .models import BuildDeFiRedeemTransactionResponse
from .models import BuildLpAddTransactionResponse
from .models import BuildLpRemoveTransactionResponse
from .models import CalculateLpAddPairedAmountsResponse
from .models import BuildLpAddTransactionRequestTokenListInner
from .models import BuildDeFiClaimTransactionClaimTypeEnum
from .models import BuildDeFiDepositTransactionRequestToken
from .models import BuildDeFiRedeemTransactionRequestToken
from .models import GetCandlesResponse
from .models import GetHoldersRankingResponse
from .models import GetHotTokenListResponse
from .models import GetSupportedChainsResponse
from .models import GetTokenAdvancedInfoResponse
from .models import GetTokenBasicInfoResponse
from .models import GetTokenDevInfoResponse
from .models import GetTokenPriceResponse
from .models import GetTokenTradesResponse
from .models import GetTokenTradingInfoResponse
from .models import GetTopLiquidityPoolsResponse
from .models import GetTopTradersResponse
from .models import SearchTokenResponse
from .models import GetCandlesBarEnum
from .models import GetHoldersRankingTagFilterEnum
from .models import GetHotTokenListRankByEnum
from .models import GetHotTokenListRankingTimeFrameEnum
from .models import GetTokenTradesTagFilterEnum
from .models import GetTopTradersTagFilterEnum
from .models import GetRwaTokenIssuancePlatformsResponse
from .models import GetRwaTokenListResponse
from .models import GetRwaTokenPriceResponse
from .models import GetRwaUnderlyingInfoResponse
from .models import GetRwaUnderlyingMarketDataResponse
from .models import SearchRwaTokenResponse
from .models import GetRwaTokenIssuancePlatformsPlatformIdEnum
from .models import GetRwaTokenListPlatformIdEnum
from .models import SearchRwaTokenPlatformIdEnum
from .models import BuildSolanaSwapInstructionsResponse
from .models import BuildSwapTransactionResponse
from .models import GetAggregatedQuoteResponse
from .models import GetAggregatorSupportedChainsResponse
from .models import GetErc20ApproveTransactionResponse
from .models import GetRfqOrderStatusResponse
from .models import GetTransactionStatusResponse
from .models import QuoteAndBuildSwapTransactionResponse
from .models import SubmitRfqOrderResponse
from .models import BuildSolanaSwapInstructionsBinanceChainIdEnum
from .models import BuildSolanaSwapInstructionsAutoSlippageEnum
from .models import BuildSolanaSwapInstructionsGasLevelEnum
from .models import BuildSwapTransactionApproveTransactionEnum
from .models import BuildSwapTransactionGasLevelEnum
from .models import BuildSwapTransactionAutoSlippageEnum
from .models import GetAggregatedQuoteVendorEnum
from .models import GetAggregatedQuoteFeeSourceEnum
from .models import QuoteAndBuildSwapTransactionVendorEnum
from .models import QuoteAndBuildSwapTransactionApproveTransactionEnum
from .models import QuoteAndBuildSwapTransactionGasLevelEnum
from .models import QuoteAndBuildSwapTransactionAutoSlippageEnum
from .models import SubmitRfqOrderVendorEnum
from .models import BroadcastTransactionsResponse
from .models import GetBroadcastOrdersResponse
from .models import GetGasLimitResponse
from .models import GetGasPriceResponse
from .models import GetLatestBlockHeightResponse
from .models import GetTransactionSupportedChainsResponse
from .models import SimulateTransactionsResponse
from .models import GetGasLimitRequestEvmTx
from .models import GetGasLimitRequestSolTx
from .models import GetGasLimitRequestTronTx
from .models import SimulateTransactionsRequestEvmTx
from .models import SimulateTransactionsRequestSolTx
from .models import SimulateTransactionsRequestTronTx
from .models import GetAllTokenBalancesByAddressResponse
from .models import GetTokenBalancesByAddressResponse
from .models import GetTransactionDetailByHashResponse
from .models import GetTransactionsByAddressResponse
from .models import GetWalletSupportedChainsResponse
from .models import GetTokenBalancesByAddressRequestTokenContractAddressesInner
from .models import GetTokenBalancesByAddressExcludeRiskTokenEnum


T = TypeVar("T")


class Web3WalletRestAPI:
    def __init__(
        self,
        configuration: ConfigurationRestAPI,
    ) -> None:
        self.configuration = configuration
        self._session = requests.Session()
        self._signer = (
            Signers.get_signer(
                configuration.private_key, configuration.private_key_passphrase
            )
            if configuration.private_key is not None
            else None
        )

        self._addressPortfolioApi = AddressPortfolioApi(
            self.configuration, self._session, self._signer
        )
        self._b402PaymentsApi = B402PaymentsApi(
            self.configuration, self._session, self._signer
        )
        self._defiDataApi = DefiDataApi(self.configuration, self._session, self._signer)
        self._defiTransactionApi = DefiTransactionApi(
            self.configuration, self._session, self._signer
        )
        self._generalDataApi = GeneralDataApi(
            self.configuration, self._session, self._signer
        )
        self._rWADataApi = RWADataApi(self.configuration, self._session, self._signer)
        self._tradingApi = TradingApi(self.configuration, self._session, self._signer)
        self._transactionApi = TransactionApi(
            self.configuration, self._session, self._signer
        )
        self._walletApi = WalletApi(self.configuration, self._session, self._signer)

    def send_request(
        self,
        endpoint: str,
        method: str,
        query_params: Optional[dict] = None,
        body_params: Optional[dict] = None,
    ) -> ApiResponse[T]:
        """
        Sends an request to the Binance REST API.

        Args:
            endpoint (str): The API endpoint path to send the request to.
            method (str): The HTTP method to use for the request (e.g. "GET", "POST", "PUT", "DELETE").
            query_params (Optional[dict]): The request payload as a dictionary, or None if no payload is required.
            body_params (Optional[dict]): The request body as a dictionary, or None if no body is required.

        Returns:
            ApiResponse[T]: The API response, where T is the expected response type.
        """
        return send_request[T](
            self._session,
            self.configuration,
            method,
            endpoint,
            query_params,
            body_params,
        )

    def send_signed_request(
        self,
        endpoint: str,
        method: str,
        query_params: Optional[dict] = None,
        body_params: Optional[dict] = None,
    ) -> ApiResponse[T]:
        """
        Sends a signed request to the Binance REST API.

        Args:
            endpoint (str): The API endpoint path to send the request to.
            method (str): The HTTP method to use for the request (e.g. "GET", "POST", "PUT", "DELETE").
            query_params (Optional[dict]): The request payload as a dictionary, or None if no payload is required.
            body_params (Optional[dict]): The request body as a dictionary, or None if no body is required.

        Returns:
            ApiResponse[T]: The API response, where T is the expected response type.
        """
        return send_request[T](
            self._session,
            self.configuration,
            method,
            endpoint,
            query_params,
            body_params,
            is_signed=True,
            signer=self._signer,
        )

    def get_address_pn_l_for_specific_token(
        self,
        binance_chain_id: Union[str, None],
        wallet_address: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetAddressPnLForSpecificTokenResponse]:
        """
        Get Address PnL for Specific Token

        Return the latest realized PnL detail for a specific token held by an address, including buy/sell stats, current balance, and holding duration.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            wallet_address (Union[str, None]): Wallet address to query. EVM chains accept lowercase 42-character hex addresses; Solana accepts base58 addresses (case-sensitive).
            token_contract_address (Union[str, None]): Token contract address. EVM chains accept lowercase 42-character hex addresses; Solana accepts base58 addresses (case-sensitive).
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetAddressPnLForSpecificTokenResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._addressPortfolioApi.get_address_pn_l_for_specific_token(
            binance_chain_id, wallet_address, token_contract_address, recv_window, nonce
        )

    def get_address_portfolio_overview(
        self,
        binance_chain_id: Union[str, None],
        wallet_address: Union[str, None],
        time_frame: Union[GetAddressPortfolioOverviewTimeFrameEnum, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetAddressPortfolioOverviewResponse]:
        """
        Get Address Portfolio Overview

        Return trading performance overview for an address within the selected time frame, including realized PnL, daily PnL breakdown, win rate, and top 3 profitable tokens.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            wallet_address (Union[str, None]): Wallet address to query. EVM chains accept lowercase 42-character hex addresses; Solana accepts base58 addresses (case-sensitive).
            time_frame (Union[GetAddressPortfolioOverviewTimeFrameEnum, None]): Statistics time range. 1=1D, 2=7D, 3=1M, 4=3M. Note: these values differ from the leaderboard/list endpoint (where 1=7D, 2=1M, 3=3M); do not reuse enum knowledge across endpoints.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetAddressPortfolioOverviewResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._addressPortfolioApi.get_address_portfolio_overview(
            binance_chain_id, wallet_address, time_frame, recv_window, nonce
        )

    def get_address_recent_pn_l(
        self,
        binance_chain_id: Union[str, None],
        wallet_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[str] = None,
    ) -> ApiResponse[GetAddressRecentPnLResponse]:
        """
        Get Address Recent PnL

        Return recent realized PnL list for an address.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            wallet_address (Union[str, None]): Wallet address to query. EVM chains accept lowercase 42-character hex addresses; Solana accepts base58 addresses (case-sensitive).
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            cursor (Optional[str] = None): Pagination cursor. Omit or pass empty string for the first page; pass the cursor from the previous response to get the next page.
            limit (Optional[str] = None): Number of records per page. Default 20, max 100.

        Returns:
            ApiResponse[GetAddressRecentPnLResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._addressPortfolioApi.get_address_recent_pn_l(
            binance_chain_id, wallet_address, recv_window, nonce, cursor, limit
        )

    def get_dex_trade_history(
        self,
        binance_chain_id: Union[str, None],
        wallet_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        begin: Optional[str] = None,
        end: Optional[str] = None,
        token_contract_address: Optional[str] = None,
        type: Optional[GetDexTradeHistoryTypeEnum] = None,
        cursor: Optional[str] = None,
        limit: Optional[str] = None,
    ) -> ApiResponse[GetDexTradeHistoryResponse]:
        """
        Get DEX Trade History

        Return swap trade history for a specific address. Supports filtering by time range, token, and trade type.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            wallet_address (Union[str, None]): Wallet address to query. EVM chains accept lowercase 42-character hex addresses; Solana accepts base58 addresses (case-sensitive).
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            begin (Optional[str] = None): Start timestamp (Unix milliseconds, inclusive). If both begin and end are omitted, no time filter is applied.
            end (Optional[str] = None): End timestamp (Unix milliseconds, inclusive). If both begin and end are omitted, no time filter is applied.
            token_contract_address (Optional[str] = None): Filter by token contract address. Returns all tokens when omitted.
            type (Optional[GetDexTradeHistoryTypeEnum] = None): Trade type filter. 1=Buy, 2=Sell. Omit to return all types.
            cursor (Optional[str] = None): Pagination cursor. Omit or pass empty string for the first page.
            limit (Optional[str] = None): Number of records per page. Default 20, max 100.

        Returns:
            ApiResponse[GetDexTradeHistoryResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._addressPortfolioApi.get_dex_trade_history(
            binance_chain_id,
            wallet_address,
            recv_window,
            nonce,
            begin,
            end,
            token_contract_address,
            type,
            cursor,
            limit,
        )

    def get_leaderboard(
        self,
        binance_chain_id: Union[str, None],
        time_frame: Union[GetLeaderboardTimeFrameEnum, None],
        sort_by: Union[GetLeaderboardSortByEnum, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        wallet_type: Optional[GetLeaderboardWalletTypeEnum] = None,
        min_realized_pnl_usd: Optional[str] = None,
        max_realized_pnl_usd: Optional[str] = None,
        min_win_rate_percent: Optional[str] = None,
        max_win_rate_percent: Optional[str] = None,
        min_txs: Optional[str] = None,
        max_txs: Optional[str] = None,
        min_tx_volume: Optional[str] = None,
        max_tx_volume: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[str] = None,
    ) -> ApiResponse[GetLeaderboardResponse]:
        """
        Get Leaderboard

        Return a leaderboard of top-performing wallets, with sorting and filtering by PnL, win rate, transaction count, and volume.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana). Single chain only.
            time_frame (Union[GetLeaderboardTimeFrameEnum, None]): Statistics time range. 1=7D, 2=1M, 3=3M. Note: these values differ from the portfolio/overview endpoint (where 1=1D, 2=7D, 3=1M, 4=3M); do not reuse enum knowledge across endpoints.
            sort_by (Union[GetLeaderboardSortByEnum, None]): Sort field. 1=Realized PnL (USD), 2=Win Rate, 3=Transaction count, 4=Volume (USD), 5=Realized PnL %.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            wallet_type (Optional[GetLeaderboardWalletTypeEnum] = None): Wallet type filter, single selection only. 1=Smart Money, 2=KOL, 3=Hot List. Returns all types when omitted.
            min_realized_pnl_usd (Optional[str] = None): Minimum realized PnL filter, denominated in USD.
            max_realized_pnl_usd (Optional[str] = None): Maximum realized PnL filter, denominated in USD.
            min_win_rate_percent (Optional[str] = None): Minimum win rate filter, without percent sign. "50" means ≥50%.
            max_win_rate_percent (Optional[str] = None): Maximum win rate filter, without percent sign. "90" means ≤90%.
            min_txs (Optional[str] = None): Minimum transaction count filter.
            max_txs (Optional[str] = None): Maximum transaction count filter.
            min_tx_volume (Optional[str] = None): Minimum total transaction volume filter, denominated in USD.
            max_tx_volume (Optional[str] = None): Maximum total transaction volume filter, denominated in USD.
            cursor (Optional[str] = None): Pagination cursor. Omit or pass empty string for the first page.
            limit (Optional[str] = None): Number of records per page. Default 50, max 50.

        Returns:
            ApiResponse[GetLeaderboardResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._addressPortfolioApi.get_leaderboard(
            binance_chain_id,
            time_frame,
            sort_by,
            recv_window,
            nonce,
            wallet_type,
            min_realized_pnl_usd,
            max_realized_pnl_usd,
            min_win_rate_percent,
            max_win_rate_percent,
            min_txs,
            max_txs,
            min_tx_volume,
            max_tx_volume,
            cursor,
            limit,
        )

    def get_portfolio_supported_chains(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetPortfolioSupportedChainsResponse]:
        """
        Get Portfolio Supported Chains

        Return the list of blockchains supported by address portfolio analysis.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetPortfolioSupportedChainsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._addressPortfolioApi.get_portfolio_supported_chains(
            recv_window, nonce
        )

    def get_tracked_trades(
        self,
        tracker_type: Union[GetTrackedTradesTrackerTypeEnum, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        wallet_address: Optional[str] = None,
        trade_type: Optional[str] = None,
        binance_chain_id: Optional[str] = None,
        min_volume: Optional[str] = None,
        max_volume: Optional[str] = None,
        min_market_cap: Optional[str] = None,
        max_market_cap: Optional[str] = None,
        is_hide_risk_token: Optional[bool] = None,
        limit: Optional[int] = None,
    ) -> ApiResponse[GetTrackedTradesResponse]:
        """
        Get Tracked Trades

        Return the latest swap trades from tracked addresses (smart money, KOL, or custom address list).

        Args:
            tracker_type (Union[GetTrackedTradesTrackerTypeEnum, None]): Tracker type. 1=Smart Money, 2=KOL, 3=Custom address list (walletAddress required).
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            wallet_address (Optional[str] = None): Required when trackerType=3. One or more wallet addresses, comma-separated, up to 20. EVM chains accept lowercase 42-character hex addresses; Solana accepts base58 addresses (case-sensitive).
            trade_type (Optional[str] = None): Trade type filter. 0=All (default), 1=Buy, 2=Sell, 3=Add position, 4=Reduce position, 5=Open position, 6=Close position, 7=Transfer in, 8=Transfer out. Multiple values comma-separated.
            binance_chain_id (Optional[str] = None): Filter by chain. Returns all chains when omitted.
            min_volume (Optional[str] = None): Minimum trade volume filter, denominated in USD.
            max_volume (Optional[str] = None): Maximum trade volume filter, denominated in USD.
            min_market_cap (Optional[str] = None): Minimum market cap filter, denominated in USD.
            max_market_cap (Optional[str] = None): Maximum market cap filter, denominated in USD.
            is_hide_risk_token (Optional[bool] = None): Whether to hide risk tokens. true=hide risk tokens (default), false=show all tokens including risk tokens.
            limit (Optional[int] = None): Maximum number of trades to return. Effective only for trackerType=3 (custom address list); trackerType=1/2 ignore this and use the downstream public query fixed cap. Omit to use the downstream default.

        Returns:
            ApiResponse[GetTrackedTradesResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._addressPortfolioApi.get_tracked_trades(
            tracker_type,
            recv_window,
            nonce,
            wallet_address,
            trade_type,
            binance_chain_id,
            min_volume,
            max_volume,
            min_market_cap,
            max_market_cap,
            is_hide_risk_token,
            limit,
        )

    def get_b402_supported_configurations_v1(
        self,
        body: Union[object, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetB402SupportedConfigurationsV1Response]:
        """
        Get B402 Supported Configurations V1

        Legacy V1 compatibility endpoint. New integrations should use V2. V1 returns `x402Version=1`, method-specific `facilitatorAddress`, and V1 signer-map semantics.

        Args:
            body (Union[object, None]):
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetB402SupportedConfigurationsV1Response]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._b402PaymentsApi.get_b402_supported_configurations_v1(
            body, recv_window, nonce
        )

    def get_b402_supported_configurations_v2(
        self,
        body: Union[object, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetB402SupportedConfigurationsV2Response]:
        """
        Get B402 Supported Configurations V2

        Return the live x402 V2 payment kinds available to the authenticated Developer Portal project. Use the result to construct HTTP 402 payment requirements. Copy the selected `extra` object verbatim and refresh cached configuration periodically.

        Args:
            body (Union[object, None]):
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetB402SupportedConfigurationsV2Response]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._b402PaymentsApi.get_b402_supported_configurations_v2(
            body, recv_window, nonce
        )

    def settle_b402_payment_v1(
        self,
        body: Union[B402SettleEnvelopeV1Body, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[SettleB402PaymentV1Response]:
        """
        Settle B402 Payment V1

        Legacy V1 on-chain settlement endpoint. New integrations should use V2. This operation can move real funds and is irreversible. V1 additionally returns `confirmations` in the settlement result.

        Args:
            body (Union[B402SettleEnvelopeV1Body, None]):
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[SettleB402PaymentV1Response]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._b402PaymentsApi.settle_b402_payment_v1(body, recv_window, nonce)

    def settle_b402_payment_v2(
        self,
        body: Union[B402SettleEnvelopeV2Body, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[SettleB402PaymentV2Response]:
        """
        Settle B402 Payment V2

        Submit a verified x402 V2 authorization on-chain. This operation can move real funds and is irreversible. For `permit2-upto`, provide `settleAmount` in atomic units. Business failure is returned as HTTP 200 with `data.success=false`; reconcile any non-empty transaction hash before retrying.

        Args:
            body (Union[B402SettleEnvelopeV2Body, None]):
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[SettleB402PaymentV2Response]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._b402PaymentsApi.settle_b402_payment_v2(body, recv_window, nonce)

    def verify_b402_payment_v1(
        self,
        body: Union[VerifyB402PaymentV1RequestBody, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[VerifyB402PaymentV1Response]:
        """
        Verify B402 Payment V1

        Legacy V1 off-chain verification endpoint. New integrations should use V2. Portal identity is resolved from the API Key project, so `merchantId` is omitted from the external request.

        Args:
            body (Union[VerifyB402PaymentV1RequestBody, None]):
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[VerifyB402PaymentV1Response]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._b402PaymentsApi.verify_b402_payment_v1(body, recv_window, nonce)

    def verify_b402_payment_v2(
        self,
        body: Union[VerifyB402PaymentV2RequestBody, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[VerifyB402PaymentV2Response]:
        """
        Verify B402 Payment V2

        Validate an x402 V2 payment authorization off-chain without spending funds or broadcasting a transaction. Portal identity is resolved from the authenticated API Key project; omit `merchantId`. A validation failure is returned as HTTP 200 with `data.isValid=false`.

        Args:
            body (Union[VerifyB402PaymentV2RequestBody, None]):
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[VerifyB402PaymentV2Response]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._b402PaymentsApi.verify_b402_payment_v2(body, recv_window, nonce)

    def get_de_fi_positions(
        self,
        addresses: Union[List[str], None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        binance_chain_ids: Optional[List[str]] = None,
    ) -> ApiResponse[GetDeFiPositionsResponse]:
        """
                Get DeFi Positions

                Query DeFi positions for one or more wallet addresses on BSC. Returns protocol-level position summaries with token breakdowns. Optionally filter by specific chains via `binanceChainIds`.
        Position coverage is wider than the protocol list used by data queries and transaction building — see [Supported Chains & Protocols](../supported-chains) for the difference.

                Args:
                    addresses (Union[List[str], None]): Wallet addresses to query positions for. Up to 3 addresses per request (server-side configured and subject to change).
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    binance_chain_ids (Optional[List[str]] = None): Optional chain filter. When set, only positions on the specified chains are returned. This release supports BSC ("56") only.

                Returns:
                    ApiResponse[GetDeFiPositionsResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._defiDataApi.get_de_fi_positions(
            addresses, recv_window, nonce, binance_chain_ids
        )

    def get_investment_detail(
        self,
        investment_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetInvestmentDetailResponse]:
        """
        Get Investment Detail

        Get detailed information for a specific DeFi investment product, including APY, TVL, supported tokens (asset / reward / LP / borrow), pool address, fee rate, and investability.

        Args:
            investment_id (Union[str, None]): Investment product identifier (64-char hex, no `0x` prefix). Same format as the `investmentIds` returned by `POST /api/v1/defi/data/position/list`.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetInvestmentDetailResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._defiDataApi.get_investment_detail(
            investment_id, recv_window, nonce
        )

    def get_protocol_detail(
        self,
        defi_protocol_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetProtocolDetailResponse]:
        """
        Get Protocol Detail

        Get detailed information for a specific DeFi protocol, including description, highlights, CertiK security scores, team, fundraising, social links, and FAQ.

        Args:
            defi_protocol_id (Union[str, None]): DeFi protocol identifier.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetProtocolDetailResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._defiDataApi.get_protocol_detail(
            defi_protocol_id, recv_window, nonce
        )

    def list_de_fi_investments(
        self,
        invest_type: Union[ListDeFiInvestmentsInvestTypeEnum, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        defi_protocol_id: Optional[str] = None,
        token_address_list: Optional[List[str]] = None,
        binance_chain_id: Optional[str] = None,
        sort_field: Optional[str] = None,
        sort_direction: Optional[ListDeFiInvestmentsSortDirectionEnum] = None,
        page: Optional[int] = None,
        size: Optional[int] = None,
    ) -> ApiResponse[ListDeFiInvestmentsResponse]:
        """
        List DeFi Investments

        List available DeFi investment products. Filter by protocol, chain, investment type, or contract address. Results are paginated and sortable.

        Args:
            invest_type (Union[ListDeFiInvestmentsInvestTypeEnum, None]): Filter by investment type. Required. Known values: `Earn`, `LiquidityPool`.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            defi_protocol_id (Optional[str] = None): Filter by DeFi protocol ID.
            token_address_list (Optional[List[str]] = None): Filter by token addresses.
            binance_chain_id (Optional[str] = None): Filter by Binance chain ID (e.g. "56"=BSC).
            sort_field (Optional[str] = None): Field to sort by. Only two values are accepted: `tvl`, `apy`. Note: sorting by APY uses `apy` (the server-side field name), even though the response exposes `apyBps` / `apyDisplay`. Other values are not supported.
            sort_direction (Optional[ListDeFiInvestmentsSortDirectionEnum] = None): Sort direction.
            page (Optional[int] = None): Page number (1-based).
            size (Optional[int] = None): Page size. Max: 100.

        Returns:
            ApiResponse[ListDeFiInvestmentsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._defiDataApi.list_de_fi_investments(
            invest_type,
            recv_window,
            nonce,
            defi_protocol_id,
            token_address_list,
            binance_chain_id,
            sort_field,
            sort_direction,
            page,
            size,
        )

    def list_de_fi_protocols(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        binance_chain_id: Optional[str] = None,
        invest_type: Optional[ListDeFiProtocolsInvestTypeEnum] = None,
        sort_field: Optional[str] = None,
        sort_direction: Optional[ListDeFiProtocolsSortDirectionEnum] = None,
        page: Optional[int] = None,
        size: Optional[int] = None,
    ) -> ApiResponse[ListDeFiProtocolsResponse]:
        """
        List DeFi Protocols

        List supported DeFi protocols with optional filtering by chain and investment type. Results are paginated and sortable.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            binance_chain_id (Optional[str] = None): Filter by Binance chain ID (e.g. "56"=BSC).
            invest_type (Optional[ListDeFiProtocolsInvestTypeEnum] = None): Filter by investment type. Known values: `Earn`, `LiquidityPool`.
            sort_field (Optional[str] = None): Field to sort by. Only two values are accepted: `tvl`, `apy`. Note: sorting by APY uses `apy` (the server-side field name), even though the response exposes `apyBps` / `apyDisplay`. Other values are not supported.
            sort_direction (Optional[ListDeFiProtocolsSortDirectionEnum] = None): Sort direction.
            page (Optional[int] = None): Page number (1-based).
            size (Optional[int] = None): Page size. Max: 200.

        Returns:
            ApiResponse[ListDeFiProtocolsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._defiDataApi.list_de_fi_protocols(
            recv_window,
            nonce,
            binance_chain_id,
            invest_type,
            sort_field,
            sort_direction,
            page,
            size,
        )

    def build_de_fi_claim_transaction(
        self,
        address: Union[str, None],
        claim_type: Union[BuildDeFiClaimTransactionClaimTypeEnum, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        binance_chain_id: Optional[str] = None,
        investment_id: Optional[str] = None,
        defi_protocol_id: Optional[str] = None,
        nft_id: Optional[str] = None,
        redemption_id: Optional[str] = None,
        token_address_list: Optional[List[str]] = None,
        simulate: Optional[bool] = None,
    ) -> ApiResponse[BuildDeFiClaimTransactionResponse]:
        """
                Build DeFi Claim Transaction

                Build the unsigned claim calldata for one of four claim types — see `DefiClaimType` for which companion fields each type requires **and which protocol constraints apply** (e.g. `REWARD_PROTOCOL` is not supported for LP protocols; PancakeSwap V3 farming uses `LP_FEE`, not `REWARD_INVESTMENT`). Returns an ordered `dataList` (typically `[CLAIM]`).
        **`binanceChainId` resolution rules**:
        - Normally resolved from `investmentId`; the call always executes on that chain.
        - For claim types other than `REWARD_PROTOCOL`, any client-supplied `binanceChainId` is
          **silently ignored** — passing a different chainId will not redirect the claim.

        - **Exception**: `REWARD_PROTOCOL` with no `investmentId` — the client MUST pass
          `binanceChainId` (it is the only chain signal).

        `tokenAddressList` optionally narrows the claim scope. Set `simulate=true` to also receive `preview`.

                Args:
                    address (Union[str, None]): User wallet address (the signer / sender).
                    claim_type (Union[BuildDeFiClaimTransactionClaimTypeEnum, None]): Claim routing type. Determines which companion fields are required: - `REWARD_PROTOCOL`: protocol-level rewards (e.g. Aave incentive, Radiant) — requires `defiProtocolId` + `binanceChainId` - `REWARD_INVESTMENT`: per-investment rewards — requires `investmentId` - `LP_FEE`: Uniswap V3/V4 LP fees — requires `investmentId` + `nftId` - `REDEMPTION`: redeem principal (Lista / Aster / Solv etc.) — requires `investmentId` + `redemptionId`  **Protocol constraints** — not every `claimType` is valid for every protocol; an invalid combination is rejected (`40453` or `40454`): - `REWARD_PROTOCOL` is **not** supported for LP (liquidity-pool) protocols in this release. - `REWARD_INVESTMENT` is not supported for PancakeSwap V3 farming positions; use `LP_FEE`   to claim farming incentives there. - `LP_FEE` applies only to concentrated-liquidity (V3/V4) LP positions. - `REDEMPTION` applies only to protocols with a redemption / lock-up flow (Lista, Aster, Solv).  The valid `claimType`s for a specific position are returned in that position's `positionDetail.claimTypes` (see the position list) — branch on that rather than hard-coding the mapping. Note: error messages may surface the protocol id with a chain prefix (e.g. `bsc_pancakeswap4`); the canonical id in API fields is the un-prefixed form (e.g. `pancakeswap4`).
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    binance_chain_id (Optional[str] = None): Binance chain ID. For `REWARD_PROTOCOL` (which does not use `investmentId`) this is the only chain signal and is required. For all other claim types the chain is resolved from `investmentId` and any client-supplied value is silently ignored — the call still executes on the `investmentId` chain, so a different chainId does not redirect the claim.
                    investment_id (Optional[str] = None): Required for `REWARD_INVESTMENT`, `LP_FEE`, and `REDEMPTION`. Not used for `REWARD_PROTOCOL` (which keys off `defiProtocolId`).
                    defi_protocol_id (Optional[str] = None): Required for `REWARD_PROTOCOL` (e.g. `pancakeswap3`).
                    nft_id (Optional[str] = None): Required for `LP_FEE`. The NFT token ID as a decimal string (e.g. `"7225901"` or `"#7225901"`). The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
                    redemption_id (Optional[str] = None): Required for `REDEMPTION`. Maps to the `redemptionId` returned in the position list (the on-line position index).
                    token_address_list (Optional[List[str]] = None): Optional token address filter to narrow the claim scope.
                    simulate (Optional[bool] = None): When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).

                Returns:
                    ApiResponse[BuildDeFiClaimTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._defiTransactionApi.build_de_fi_claim_transaction(
            address,
            claim_type,
            recv_window,
            nonce,
            binance_chain_id,
            investment_id,
            defi_protocol_id,
            nft_id,
            redemption_id,
            token_address_list,
            simulate,
        )

    def build_de_fi_deposit_transaction(
        self,
        address: Union[str, None],
        investment_id: Union[str, None],
        token: Union[BuildDeFiDepositTransactionRequestToken, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        simulate: Optional[bool] = None,
    ) -> ApiResponse[BuildDeFiDepositTransactionResponse]:
        """
                Build DeFi Deposit Transaction

                Build the unsigned transaction calldata for a DeFi deposit / stake. Returns an ordered `dataList` (typically `[APPROVE, DEPOSIT]`, or `[DEPOSIT]` when the allowance is already sufficient) for the caller to sign and broadcast.
        The caller does NOT pass `binanceChainId` — the service resolves it from `investmentId`. Set `simulate=true` to also receive a `preview` (projected balance change, estimated gas, and lending health-factor change) without broadcasting.

                Args:
                    address (Union[str, None]): User wallet address (the signer / sender).
                    investment_id (Union[str, None]): Investment product ID (chain-agnostic). The service resolves the chain from this ID.
                    token (Union[BuildDeFiDepositTransactionRequestToken, None]):
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    simulate (Optional[bool] = None): When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).

                Returns:
                    ApiResponse[BuildDeFiDepositTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._defiTransactionApi.build_de_fi_deposit_transaction(
            address, investment_id, token, recv_window, nonce, simulate
        )

    def build_de_fi_redeem_transaction(
        self,
        address: Union[str, None],
        investment_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        token: Optional[BuildDeFiRedeemTransactionRequestToken] = None,
        ratio: Optional[str] = None,
        slippage_bps: Optional[str] = None,
        simulate: Optional[bool] = None,
    ) -> ApiResponse[BuildDeFiRedeemTransactionResponse]:
        """
                Build DeFi Redeem Transaction

                Build the unsigned transaction calldata for a DeFi redeem / withdraw. Returns an ordered `dataList` (typically `[REDEEM]` or `[APPROVE, REDEEM]`).
        **Redeem amount is decided by exactly one of `ratio` or `token`** (mutually exclusive): - `ratio` omitted / blank → **exact-amount redeem**: the amount comes from
          `token` (`tokenAddress` + `amount`); the position is *not* queried. `token` is required
          in this mode (omitting `token`, or leaving `amount` blank, is rejected with `40001`,
          `Parameter error`; both `ratio` and `token` absent is also rejected with `40001`).
        - `ratio` present → **proportional redeem**: must parse as a decimal in `(0, 1]`. The
          service queries the user's on-chain position under `investmentId` and scales each
          token's amount by `ratio` (floored to the token's decimals). `ratio="1"` is a full (max)
          redeem. In this mode `token` is **ignored** — the output tokens and amounts are derived
          from the position.

        `slippageBps` is the slippage tolerance in basis points (e.g. `300` = 3%). The caller does NOT pass `binanceChainId` — resolved from `investmentId`. Set `simulate=true` to also receive `preview`.
        The response also carries `redeemDelayDays` — the redeem waiting period as a `[min, max]` day pair (e.g. `["7","10"]` = 7–10 days, `[]` = instant). The wait starts after the redeem tx is confirmed on-chain. See the `redeemDelayDays` field on `DefiTxResponse`.

                Args:
                    address (Union[str, None]): User wallet address (the signer / sender).
                    investment_id (Union[str, None]): Investment product ID (chain-agnostic). The service resolves the chain from this ID.
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    token (Optional[BuildDeFiRedeemTransactionRequestToken] = None):
                    ratio (Optional[str] = None): Redeem ratio, mutually exclusive with `token`. Omitted / blank → exact-amount redeem via `token` (`tokenAddress` + `amount`, position not queried). Present → must parse as a decimal in `(0, 1]`; the service queries the on-chain position under `investmentId` and scales each token amount by `ratio` (floored to decimals). `"1"` = full (max) redeem; in this mode `token` is ignored.
                    slippage_bps (Optional[str] = None): Slippage tolerance in basis points (e.g. `300` = 3%).
                    simulate (Optional[bool] = None): When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns an error code instead of a `200` with `preview.success=false`: `40485` when the redeem amount exceeds the current position, `40484` for any other revert (see Error Codes).

                Returns:
                    ApiResponse[BuildDeFiRedeemTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._defiTransactionApi.build_de_fi_redeem_transaction(
            address,
            investment_id,
            recv_window,
            nonce,
            token,
            ratio,
            slippage_bps,
            simulate,
        )

    def build_lp_add_transaction(
        self,
        address: Union[str, None],
        investment_id: Union[str, None],
        token_list: Union[List[BuildLpAddTransactionRequestTokenListInner], None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        tick_lower: Optional[str] = None,
        tick_upper: Optional[str] = None,
        price_range: Optional[str] = None,
        nft_id: Optional[str] = None,
        slippage_bps: Optional[str] = None,
        simulate: Optional[bool] = None,
    ) -> ApiResponse[BuildLpAddTransactionResponse]:
        """
                Build LP Add Transaction

                Build the unsigned transaction calldata for adding liquidity. `tokenList` supports multiple tokens. Returns an ordered `dataList` (typically `[APPROVE, LP_ADD]`).
        **Tick range source (provide exactly one group)**:
        1. `nftId` — append to an existing LP position (reuses its range).
        2. `priceRange` — percentage band (e.g. `"5"` = ±5%) for a new position.
        3. `tickLower` + `tickUpper` — explicit raw `int24` pair for a new position.
        **Rules**:
        - If more than one group is supplied, only the highest-priority one above is used and the
          others are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).

        - If none is supplied, the request is rejected (`40453`).
        - `tickLower` / `tickUpper` are raw `int24` values and MUST be aligned to the pool's
          `tickSpacing`, otherwise the request is rejected (`40453`).

        - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.

                Args:
                    address (Union[str, None]): User wallet address (the signer / sender).
                    investment_id (Union[str, None]): LP investment product ID (chain-agnostic).
                    token_list (Union[List[BuildLpAddTransactionRequestTokenListInner], None]): Tokens to supply. Two tokens for a standard LP add.
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    tick_lower (Optional[str] = None): Lower tick bound (raw `int24`) for a new position. MUST be aligned to the pool's `tickSpacing`. Pick exactly one tick-range source: `tickLower`+`tickUpper`, `priceRange`, or `nftId` (to append to an existing position). If several are supplied, the lowest-priority ones are ignored (priority: `nftId` > `priceRange` > explicit tick pair).
                    tick_upper (Optional[str] = None): Upper tick bound (raw `int24`) for a new position. MUST be aligned to the pool's `tickSpacing`. See `tickLower` for the pick-exactly-one rule.
                    price_range (Optional[str] = None): Price range as a percentage (e.g. `"5"` = ±5%) for a new position. Alternative to `tickLower`+`tickUpper`; see `tickLower` for the pick-exactly-one rule.
                    nft_id (Optional[str] = None): LP NFT token ID, decimal string (e.g. `"7225901"` or `"#7225901"`). Required when appending to an existing position (reuses its range); omit for a new position. Highest-priority tick-range source — see `tickLower` for the pick-exactly-one rule. The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
                    slippage_bps (Optional[str] = None): Slippage tolerance for the LP leg, in basis points (e.g. `300` = 3%).
                    simulate (Optional[bool] = None): When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).

                Returns:
                    ApiResponse[BuildLpAddTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._defiTransactionApi.build_lp_add_transaction(
            address,
            investment_id,
            token_list,
            recv_window,
            nonce,
            tick_lower,
            tick_upper,
            price_range,
            nft_id,
            slippage_bps,
            simulate,
        )

    def build_lp_remove_transaction(
        self,
        address: Union[str, None],
        investment_id: Union[str, None],
        nft_id: Union[str, None],
        ratio: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        slippage_bps: Optional[str] = None,
        simulate: Optional[bool] = None,
    ) -> ApiResponse[BuildLpRemoveTransactionResponse]:
        """
                Build LP Remove Transaction

                Build the unsigned transaction calldata for removing liquidity. Removal is by `nftId` + `ratio` (range `(0, 1]`) — no `tokenList` is needed; the per-token amounts are derived from the on-chain position. Returns an ordered `dataList` (typically `[LP_REMOVE]`).
        `slippageBps` is the LP-leg slippage tolerance in basis points. The caller does NOT pass `binanceChainId` — resolved from `investmentId`.

                Args:
                    address (Union[str, None]): User wallet address (the signer / sender).
                    investment_id (Union[str, None]): LP investment product ID (chain-agnostic).
                    nft_id (Union[str, None]): LP NFT token ID identifying the position to remove, decimal string (e.g. `"7225901"` or `"#7225901"`). The `#` prefix returned by the position list may be passed as-is — the service strips it automatically.
                    ratio (Union[str, None]): Removal ratio in the range `(0, 1]` as a decimal string (e.g. `"0.04"`, `"1"`).
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    slippage_bps (Optional[str] = None): Slippage tolerance for the LP leg, in basis points (e.g. `300` = 3%).
                    simulate (Optional[bool] = None): When `true`, the response also includes a `preview` object. Default `false`. If the simulated transaction reverts, the request returns `40485` for a specific classified revert reason or `40484` for any other revert (see Error Codes).

                Returns:
                    ApiResponse[BuildLpRemoveTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._defiTransactionApi.build_lp_remove_transaction(
            address,
            investment_id,
            nft_id,
            ratio,
            recv_window,
            nonce,
            slippage_bps,
            simulate,
        )

    def calculate_lp_add_paired_amounts(
        self,
        address: Union[str, None],
        investment_id: Union[str, None],
        input_token: Union[BuildDeFiDepositTransactionRequestToken, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        tick_lower: Optional[str] = None,
        tick_upper: Optional[str] = None,
        price_range: Optional[str] = None,
        nft_id: Optional[str] = None,
    ) -> ApiResponse[CalculateLpAddPairedAmountsResponse]:
        """
                Calculate LP Add Paired Amounts

                Given a single input token, calculate the paired token amount needed for an LP add. This is a **pure computation endpoint** — it does not touch the chain and does not deduct any fee. Use it before `/transaction/lp-add` to size the paired token when only one side is supplied.
        Tick range source rules are the same as `/lp-add` — provide exactly one group: `nftId` (existing position), `priceRange`, or `tickLower`+`tickUpper`.
        - If more than one group is supplied, only the highest-priority one is used and the others
          are silently ignored (priority: `nftId` > `priceRange` > explicit tick pair).

        - If none is supplied, the request is rejected.
        - The caller does NOT pass `binanceChainId` — resolved from `investmentId`.

                Args:
                    address (Union[str, None]): User wallet address.
                    investment_id (Union[str, None]): LP investment product ID (chain-agnostic).
                    input_token (Union[BuildDeFiDepositTransactionRequestToken, None]):
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    tick_lower (Optional[str] = None): Lower tick bound (raw `int24`), aligned to the pool's `tickSpacing`.
                    tick_upper (Optional[str] = None): Upper tick bound (raw `int24`), aligned to the pool's `tickSpacing`.
                    price_range (Optional[str] = None): Price range as a percentage (e.g. `"5"` = ±5%).
                    nft_id (Optional[str] = None): Required when calculating for an existing LP position.

                Returns:
                    ApiResponse[CalculateLpAddPairedAmountsResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._defiTransactionApi.calculate_lp_add_paired_amounts(
            address,
            investment_id,
            input_token,
            recv_window,
            nonce,
            tick_lower,
            tick_upper,
            price_range,
            nft_id,
        )

    def get_candles(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        bar: Optional[GetCandlesBarEnum] = None,
        after: Optional[int] = None,
        before: Optional[int] = None,
        limit: Optional[int] = None,
    ) -> ApiResponse[GetCandlesResponse]:
        """
        Get Candles

        Return candlestick (K-line) data for a token.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            bar (Optional[GetCandlesBarEnum] = None): Candlestick time granularity, default "1m" (1 minute). Available values: 1s (1 second), 5s (5 seconds), 30s (30 seconds), 1m (1 minute), 3m (3 minutes), 5m (5 minutes), 15m (15 minutes), 30m (30 minutes), 1h (1 hour), 2h (2 hours), 4h (4 hours), 6h (6 hours), 8h (8 hours), 12h (12 hours), 1d (1 day), 3d (3 days), 1w (1 week), 1M (1 month).
            after (Optional[int] = None): End time for the query, Unix millisecond timestamp. Returns candles earlier than this time (exclusive).
            before (Optional[int] = None): Start time for the query, Unix millisecond timestamp. Returns candles later than this time (exclusive).
            limit (Optional[int] = None): Number of candles to return. Defaults to 100.

        Returns:
            ApiResponse[GetCandlesResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_candles(
            binance_chain_id,
            token_contract_address,
            recv_window,
            nonce,
            bar,
            after,
            before,
            limit,
        )

    def get_holders_ranking(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        tag_filter: Optional[GetHoldersRankingTagFilterEnum] = None,
    ) -> ApiResponse[GetHoldersRankingResponse]:
        """
        Get Holders Ranking

        Return the holders ranking list for a token, including holding amount, holding percentage, native-token balance, average buy/sell price, realized PnL, and funding source. Up to 100 records. Pagination is not supported.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier.
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            tag_filter (Optional[GetHoldersRankingTagFilterEnum] = None): Address tag filter, single selection only. Returns all holder addresses (by holding amount descending) when omitted; returns addresses matching the specified tag when provided. Mapping: 1=KOL, 2=Developer, 3=Smart Money, 4=Insider, 5=Sniper, 6=Bundler.

        Returns:
            ApiResponse[GetHoldersRankingResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_holders_ranking(
            binance_chain_id, token_contract_address, recv_window, nonce, tag_filter
        )

    def get_hot_token_list(
        self,
        binance_chain_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        rank_by: Optional[GetHotTokenListRankByEnum] = None,
        ranking_time_frame: Optional[GetHotTokenListRankingTimeFrameEnum] = None,
        price_change_percent_min: Optional[str] = None,
        price_change_percent_max: Optional[str] = None,
        volume_min: Optional[str] = None,
        volume_max: Optional[str] = None,
        txs_min: Optional[str] = None,
        txs_max: Optional[str] = None,
        market_cap_min: Optional[str] = None,
        market_cap_max: Optional[str] = None,
        liquidity_min: Optional[str] = None,
        liquidity_max: Optional[str] = None,
        dev_holding_percent_min: Optional[str] = None,
        dev_holding_percent_max: Optional[str] = None,
        inflow_usd_min: Optional[str] = None,
        inflow_usd_max: Optional[str] = None,
        holders_min: Optional[str] = None,
        holders_max: Optional[str] = None,
        bn_holder_count_min: Optional[str] = None,
        bn_holder_count_max: Optional[str] = None,
        top10_holding_percent_min: Optional[str] = None,
        top10_holding_percent_max: Optional[str] = None,
        sniper_holding_percent_min: Optional[str] = None,
        sniper_holding_percent_max: Optional[str] = None,
        smart_money_holding_percent_min: Optional[str] = None,
        smart_money_holding_percent_max: Optional[str] = None,
        kol_holding_percent_min: Optional[str] = None,
        kol_holding_percent_max: Optional[str] = None,
        pro_holding_percent_min: Optional[str] = None,
        pro_holding_percent_max: Optional[str] = None,
        fresh_wallet_holding_percent_min: Optional[str] = None,
        fresh_wallet_holding_percent_max: Optional[str] = None,
        insider_holding_percent_min: Optional[str] = None,
        insider_holding_percent_max: Optional[str] = None,
        bundler_holding_percent_min: Optional[str] = None,
        bundler_holding_percent_max: Optional[str] = None,
        dev_created_token_count_min: Optional[str] = None,
        dev_created_token_count_max: Optional[str] = None,
        dev_migrated_token_count_min: Optional[str] = None,
        dev_migrated_token_count_max: Optional[str] = None,
        dev_migrated_token_percent_min: Optional[str] = None,
        dev_migrated_token_percent_max: Optional[str] = None,
        is_dev_sold_all: Optional[bool] = None,
        is_dev_burned: Optional[bool] = None,
        is_mint: Optional[bool] = None,
        is_freeze: Optional[bool] = None,
        is_hide_wash_trading_tokens: Optional[bool] = None,
        is_hide_dev_wash_trading_tokens: Optional[bool] = None,
        is_hide_internal_wash_trading_tokens: Optional[bool] = None,
        page_id: Optional[int] = None,
        size: Optional[int] = None,
    ) -> ApiResponse[GetHotTokenListResponse]:
        """
        Get Hot Token List

        Get hot token ranking list. Supports sorting by volume, price change, market cap, etc., with filters for holding distribution, developer behavior, liquidity, and more.

        Args:
            binance_chain_id (Union[str, None]): Chain identifier. Single value only.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            rank_by (Optional[GetHotTokenListRankByEnum] = None): Ranking field. 1=price, 2=price change, 3=transactions, 4=volume, 5=market cap, 6=liquidity, 7=creation time, 8=holders, 9=Binance MPC wallet holders, 10=net inflow.
            ranking_time_frame (Optional[GetHotTokenListRankingTimeFrameEnum] = None): Data time range. 1=1 min, 2=5 min, 3=1 hour, 4=4 hours, 5=24 hours. Default 1 hour.
            price_change_percent_min (Optional[str] = None): Minimum price change filter. "5" means 5%.
            price_change_percent_max (Optional[str] = None): Maximum price change filter. "100" means 100%.
            volume_min (Optional[str] = None): Minimum volume filter, denominated in USD.
            volume_max (Optional[str] = None): Maximum volume filter, denominated in USD.
            txs_min (Optional[str] = None): Minimum transaction count.
            txs_max (Optional[str] = None): Maximum transaction count.
            market_cap_min (Optional[str] = None): Minimum market cap filter, denominated in USD.
            market_cap_max (Optional[str] = None): Maximum market cap filter, denominated in USD.
            liquidity_min (Optional[str] = None): Minimum liquidity filter, denominated in USD.
            liquidity_max (Optional[str] = None): Maximum liquidity filter, denominated in USD.
            dev_holding_percent_min (Optional[str] = None): Minimum developer holding percentage filter. "5" means 5%.
            dev_holding_percent_max (Optional[str] = None): Maximum developer holding percentage filter. "5" means 5%.
            inflow_usd_min (Optional[str] = None): Minimum net inflow filter, denominated in USD.
            inflow_usd_max (Optional[str] = None): Maximum net inflow filter, denominated in USD.
            holders_min (Optional[str] = None): Minimum holder count.
            holders_max (Optional[str] = None): Maximum holder count.
            bn_holder_count_min (Optional[str] = None): Minimum Binance MPC wallet holders.
            bn_holder_count_max (Optional[str] = None): Maximum Binance MPC wallet holders.
            top10_holding_percent_min (Optional[str] = None): Minimum top-10 holding percentage. "5" means 5%.
            top10_holding_percent_max (Optional[str] = None): Maximum top-10 holding percentage. "5" means 5%.
            sniper_holding_percent_min (Optional[str] = None): Minimum sniper holding percentage. "5" means 5%.
            sniper_holding_percent_max (Optional[str] = None): Maximum sniper holding percentage. "5" means 5%.
            smart_money_holding_percent_min (Optional[str] = None): Minimum smart-money holding percentage. "5" means 5%.
            smart_money_holding_percent_max (Optional[str] = None): Maximum smart-money holding percentage. "5" means 5%.
            kol_holding_percent_min (Optional[str] = None): Minimum KOL holding percentage. "5" means 5%.
            kol_holding_percent_max (Optional[str] = None): Maximum KOL holding percentage. "5" means 5%.
            pro_holding_percent_min (Optional[str] = None): Minimum pro-trader holding percentage. "5" means 5%.
            pro_holding_percent_max (Optional[str] = None): Maximum pro-trader holding percentage. "5" means 5%.
            fresh_wallet_holding_percent_min (Optional[str] = None): Minimum fresh-wallet holding percentage. "5" means 5%.
            fresh_wallet_holding_percent_max (Optional[str] = None): Maximum fresh-wallet holding percentage. "5" means 5%.
            insider_holding_percent_min (Optional[str] = None): Minimum insider holding percentage. "5" means 5%.
            insider_holding_percent_max (Optional[str] = None): Maximum insider holding percentage. "5" means 5%.
            bundler_holding_percent_min (Optional[str] = None): Minimum bundler holding percentage. "5" means 5%.
            bundler_holding_percent_max (Optional[str] = None): Maximum bundler holding percentage. "5" means 5%.
            dev_created_token_count_min (Optional[str] = None): Minimum developer-created token count.
            dev_created_token_count_max (Optional[str] = None): Maximum developer-created token count.
            dev_migrated_token_count_min (Optional[str] = None): Minimum developer-migrated token count.
            dev_migrated_token_count_max (Optional[str] = None): Maximum developer-migrated token count.
            dev_migrated_token_percent_min (Optional[str] = None): Minimum developer-migrated token percentage. "5" means 5%.
            dev_migrated_token_percent_max (Optional[str] = None): Maximum developer-migrated token percentage. "5" means 5%.
            is_dev_sold_all (Optional[bool] = None): When set to true, returns only tokens where the developer has sold all holdings. Not filtered when omitted or false.
            is_dev_burned (Optional[bool] = None): When set to true, returns only tokens where the developer has burned tokens. Not filtered when omitted or false.
            is_mint (Optional[bool] = None): When set to true, excludes tokens with minting capability. Default false.
            is_freeze (Optional[bool] = None): When set to true, excludes tokens with freeze capability. Default false.
            is_hide_wash_trading_tokens (Optional[bool] = None): When set to true, excludes tokens with wash trading behavior. Default true.
            is_hide_dev_wash_trading_tokens (Optional[bool] = None): When set to true, excludes tokens where the developer has wash trading behavior. Default true.
            is_hide_internal_wash_trading_tokens (Optional[bool] = None): When set to true, excludes tokens where insiders have wash trading behavior. Default true.
            page_id (Optional[int] = None): Pagination identifier. Do not pass on the first request; pass the page value from the previous response to get the next page.
            size (Optional[int] = None): Page size. Maximum 100. Defaults to 100.

        Returns:
            ApiResponse[GetHotTokenListResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_hot_token_list(
            binance_chain_id,
            recv_window,
            nonce,
            rank_by,
            ranking_time_frame,
            price_change_percent_min,
            price_change_percent_max,
            volume_min,
            volume_max,
            txs_min,
            txs_max,
            market_cap_min,
            market_cap_max,
            liquidity_min,
            liquidity_max,
            dev_holding_percent_min,
            dev_holding_percent_max,
            inflow_usd_min,
            inflow_usd_max,
            holders_min,
            holders_max,
            bn_holder_count_min,
            bn_holder_count_max,
            top10_holding_percent_min,
            top10_holding_percent_max,
            sniper_holding_percent_min,
            sniper_holding_percent_max,
            smart_money_holding_percent_min,
            smart_money_holding_percent_max,
            kol_holding_percent_min,
            kol_holding_percent_max,
            pro_holding_percent_min,
            pro_holding_percent_max,
            fresh_wallet_holding_percent_min,
            fresh_wallet_holding_percent_max,
            insider_holding_percent_min,
            insider_holding_percent_max,
            bundler_holding_percent_min,
            bundler_holding_percent_max,
            dev_created_token_count_min,
            dev_created_token_count_max,
            dev_migrated_token_count_min,
            dev_migrated_token_count_max,
            dev_migrated_token_percent_min,
            dev_migrated_token_percent_max,
            is_dev_sold_all,
            is_dev_burned,
            is_mint,
            is_freeze,
            is_hide_wash_trading_tokens,
            is_hide_dev_wash_trading_tokens,
            is_hide_internal_wash_trading_tokens,
            page_id,
            size,
        )

    def get_supported_chains(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetSupportedChainsResponse]:
        """
        Get Supported Chains

        Return the list of blockchains supported by the market service.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetSupportedChainsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_supported_chains(recv_window, nonce)

    def get_token_advanced_info(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTokenAdvancedInfoResponse]:
        """
        Get Token Advanced Info

        Return comprehensive metrics for a token, including creator and launch info, holding percentages by address type (smart money, KOL, sniper, bundler, fresh wallet, etc.), and token tags.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier.
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetTokenAdvancedInfoResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_token_advanced_info(
            binance_chain_id, token_contract_address, recv_window, nonce
        )

    def get_token_basic_info(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTokenBasicInfoResponse]:
        """
        Get Token Basic Info

        Return basic metadata for a token: name, symbol, logo, decimals, creator address, creation time, and tag info.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier.
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetTokenBasicInfoResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_token_basic_info(
            binance_chain_id, token_contract_address, recv_window, nonce
        )

    def get_token_dev_info(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTokenDevInfoResponse]:
        """
        Get Token Dev Info

        Return developer profile for a token, including historical token launch stats, current holding percentage, and initial funding source.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            token_contract_address (Union[str, None]): Token contract address. EVM chains accept lowercase 42-character hex addresses; Solana accepts base58 addresses (case-sensitive).
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetTokenDevInfoResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_token_dev_info(
            binance_chain_id, token_contract_address, recv_window, nonce
        )

    def get_token_price(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTokenPriceResponse]:
        """
        Get Token Price

        Get the latest price for tokens. Supports batch queries, up to 100 tokens per request.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetTokenPriceResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_token_price(recv_window, nonce)

    def get_token_trades(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
        tag_filter: Optional[GetTokenTradesTagFilterEnum] = None,
        wallet_address_filter: Optional[str] = None,
    ) -> ApiResponse[GetTokenTradesResponse]:
        """
        Get Token Trades

        Return on-chain trade history for a token. Supports tag filtering and wallet address filtering.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier.
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            cursor (Optional[str] = None): Pagination cursor. Do not pass on the first request; pass the cursor value from the previous response to get the next page.
            limit (Optional[int] = None): Number of results per page, maximum 500, defaults to 100.
            tag_filter (Optional[GetTokenTradesTagFilterEnum] = None): Address tag filter, single selection only. Returns all trades with pagination when omitted; returns only the latest 100 trades for the specified tag's addresses when provided (cursor and limit are ignored). Mapping: 1=KOL, 2=Developer, 3=Smart Money, 4=Insider, 5=Sniper, 6=Bundler, 7=Whale Holder.
            wallet_address_filter (Optional[str] = None): Filter by specific wallet addresses. Separate multiple addresses with commas. Up to 2 addresses.

        Returns:
            ApiResponse[GetTokenTradesResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_token_trades(
            binance_chain_id,
            token_contract_address,
            recv_window,
            nonce,
            cursor,
            limit,
            tag_filter,
            wallet_address_filter,
        )

    def get_token_trading_info(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTokenTradingInfoResponse]:
        """
        Get Token Trading Info

        Get token price and trading data (volume, transactions, market cap, holders, etc.). Supports batch queries for up to 100 tokens.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetTokenTradingInfoResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_token_trading_info(recv_window, nonce)

    def get_top_liquidity_pools(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTopLiquidityPoolsResponse]:
        """
        Get Top Liquidity Pools

        Return the top liquidity pools for a token, including pool name, protocol, liquidity in USD, pool address, and per-token composition.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier.
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetTopLiquidityPoolsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_top_liquidity_pools(
            binance_chain_id, token_contract_address, recv_window, nonce
        )

    def get_top_traders(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        tag_filter: Optional[GetTopTradersTagFilterEnum] = None,
    ) -> ApiResponse[GetTopTradersResponse]:
        """
        Get Top Traders

        Return the top profit-making addresses for a token, ranked by realized PnL descending. Includes holding amount, average buy/sell price, realized PnL, and funding source. Up to 100 records. Pagination is not supported.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier.
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            tag_filter (Optional[GetTopTradersTagFilterEnum] = None): Address tag filter, single selection only. Returns all top profit addresses (by realized PnL descending) when omitted; returns addresses matching the specified tag when provided. Mapping: 1=KOL, 2=Developer, 3=Smart Money, 4=Insider, 5=Sniper, 6=Bundler, 7=Whale Holder.

        Returns:
            ApiResponse[GetTopTradersResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.get_top_traders(
            binance_chain_id, token_contract_address, recv_window, nonce, tag_filter
        )

    def search_token(
        self,
        chains: Union[str, None],
        search: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[SearchTokenResponse]:
        """
        Search Token

        Search tokens by symbol or contract address.

        Args:
            chains (Union[str, None]): List of chain IDs to search, comma-separated.
            search (Union[str, None]): Search keyword. Can be a token address or token symbol.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[SearchTokenResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._generalDataApi.search_token(chains, search, recv_window, nonce)

    def get_rwa_token_issuance_platforms(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        platform_id: Optional[GetRwaTokenIssuancePlatformsPlatformIdEnum] = None,
    ) -> ApiResponse[GetRwaTokenIssuancePlatformsResponse]:
        """
        Get RWA Token Issuance Platforms

        Return the list of supported RWA token issuance platforms and their basic info.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds. Default 5000, max 60000.
            nonce (Optional[str] = None): Unique request identifier to prevent replay attacks. Falls back to X-OC-SIGN when omitted.
            platform_id (Optional[GetRwaTokenIssuancePlatformsPlatformIdEnum] = None): Platform identifier (ondo=Ondo Finance, bstock=bStocks). Optional. If not specified, returns all platforms; when specified, returns only that platform.

        Returns:
            ApiResponse[GetRwaTokenIssuancePlatformsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._rWADataApi.get_rwa_token_issuance_platforms(
            recv_window, nonce, platform_id
        )

    def get_rwa_token_list(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        binance_chain_id: Optional[str] = None,
        platform_id: Optional[GetRwaTokenListPlatformIdEnum] = None,
        tab_id: Optional[int] = None,
    ) -> ApiResponse[GetRwaTokenListResponse]:
        """
        Get RWA Token List

        Get the list of RWA tokens with underlying asset info. Supports filtering by platform and sector tab.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds. Default 5000, max 60000.
            nonce (Optional[str] = None): Unique request identifier to prevent replay attacks. Falls back to X-OC-SIGN when omitted.
            binance_chain_id (Optional[str] = None): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana). Optional. If not specified, returns data from all chains.
            platform_id (Optional[GetRwaTokenListPlatformIdEnum] = None): Platform filter (ondo=Ondo Finance, bstock=bStocks). Optional. If not specified, returns all platforms.
            tab_id (Optional[int] = None): Sector tab filter. Optional. If not specified, returns all sectors. Enum: 1=Serenity Call, 2=SpaceX (SpaceX-related and commercial spaceflight concept stocks), 3=Upcoming Earnings, 4=AI Chips, 5=Storage, 6=Energy, 7=Precious Metals, 8=China ADR (Chinese companies listed in the US), 9=Magnificent 7 (the seven largest US tech giants: Apple, Microsoft, Alphabet, Amazon, Nvidia, Meta, Tesla), 10=Crypto, 11=ETF, 12=Tech Leaders, 13=Buffett Portfolio.

        Returns:
            ApiResponse[GetRwaTokenListResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._rWADataApi.get_rwa_token_list(
            recv_window, nonce, binance_chain_id, platform_id, tab_id
        )

    def get_rwa_token_price(
        self,
        binance_chain_id: Union[str, None],
        token_contract_addresses: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetRwaTokenPriceResponse]:
        """
        Get RWA Token Price

        Batch query RWA token prices, including on-chain price and underlying reference price.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            token_contract_addresses (Union[str, None]): Comma-separated token contract addresses. Maximum 100 per request.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds. Default 5000, max 60000.
            nonce (Optional[str] = None): Unique request identifier to prevent replay attacks. Falls back to X-OC-SIGN when omitted.

        Returns:
            ApiResponse[GetRwaTokenPriceResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._rWADataApi.get_rwa_token_price(
            binance_chain_id, token_contract_addresses, recv_window, nonce
        )

    def get_rwa_underlying_info(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetRwaUnderlyingInfoResponse]:
        """
        Get RWA Underlying Info

        Get the underlying company information for an RWA token.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds. Default 5000, max 60000.
            nonce (Optional[str] = None): Unique request identifier to prevent replay attacks. Falls back to X-OC-SIGN when omitted.

        Returns:
            ApiResponse[GetRwaUnderlyingInfoResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._rWADataApi.get_rwa_underlying_info(
            binance_chain_id, token_contract_address, recv_window, nonce
        )

    def get_rwa_underlying_market_data(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetRwaUnderlyingMarketDataResponse]:
        """
        Get RWA Underlying Market Data

        Get market data for the underlying asset of an RWA token.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            token_contract_address (Union[str, None]): Token contract address.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds. Default 5000, max 60000.
            nonce (Optional[str] = None): Unique request identifier to prevent replay attacks. Falls back to X-OC-SIGN when omitted.

        Returns:
            ApiResponse[GetRwaUnderlyingMarketDataResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._rWADataApi.get_rwa_underlying_market_data(
            binance_chain_id, token_contract_address, recv_window, nonce
        )

    def search_rwa_token(
        self,
        keyword: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        platform_id: Optional[SearchRwaTokenPlatformIdEnum] = None,
    ) -> ApiResponse[SearchRwaTokenResponse]:
        """
        Search RWA Token

        Search RWA tokens by keyword or contract address.

        Args:
            keyword (Union[str, None]): Search keyword (ticker, company name, or contract address). Case-insensitive.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds. Default 5000, max 60000.
            nonce (Optional[str] = None): Unique request identifier to prevent replay attacks. Falls back to X-OC-SIGN when omitted.
            platform_id (Optional[SearchRwaTokenPlatformIdEnum] = None): Platform filter (ondo=Ondo Finance, bstock=bStocks). Optional. If not specified, returns results from all platforms.

        Returns:
            ApiResponse[SearchRwaTokenResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._rWADataApi.search_rwa_token(
            keyword, recv_window, nonce, platform_id
        )

    def build_solana_swap_instructions(
        self,
        binance_chain_id: Union[BuildSolanaSwapInstructionsBinanceChainIdEnum, None],
        amount: Union[str, None],
        from_token_address: Union[str, None],
        to_token_address: Union[str, None],
        slippage_percent: Union[str, None],
        user_wallet_address: Union[str, None],
        quote_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        price_impact_protection_percent: Optional[str] = None,
        auto_slippage: Optional[BuildSolanaSwapInstructionsAutoSlippageEnum] = None,
        max_auto_slippage_percent: Optional[str] = None,
        compute_unit_limit: Optional[str] = None,
        compute_unit_price: Optional[str] = None,
        gas_level: Optional[BuildSolanaSwapInstructionsGasLevelEnum] = None,
        tips: Optional[str] = None,
        fee_percent: Optional[str] = None,
        from_token_referrer_wallet_address: Optional[str] = None,
        to_token_referrer_wallet_address: Optional[str] = None,
    ) -> ApiResponse[BuildSolanaSwapInstructionsResponse]:
        """
                Build Solana Swap Instructions

                Solana-only counterpart to `/swap`. Shares the full quote → route → vendor `buildSwapTx` → priceImpact → minReceive → instruction-assembly pipeline; the only difference is the response: instead of returning a signed-and-serialized base64 transaction, this endpoint returns the **uncompiled** instruction list plus the address-lookup-table (ALT) address list, leaving v0-transaction compilation, signing, and submission to the caller.

        Use this when the caller needs to:
        - prepend / append their own instructions (e.g. fee splitting, custom
          logging) before signing;

        - reuse the platform-curated route, slippage, PS-variant rewriting, ALT
          injection, ComputeBudget overwrite, and ATA batching while still
          controlling the final wire format.


        Only supports `binanceChainId=CT_501` (Solana). Other chains return `CHAIN_NOT_SUPPORTED` (40411). Parameters mirror the Solana subset of `/swap` (no EVM-only `approveTransaction` / `approveAmount` / `gasLimit`). Supports the custom-fee (Add Fee / referral fee) parameters (`feePercent` + `fromTokenReferrerWalletAddress` / `toTokenReferrerWalletAddress`), with the same semantics as `/swap` — the fee instructions are injected into the returned uncompiled instruction list.

                Args:
                    binance_chain_id (Union[BuildSolanaSwapInstructionsBinanceChainIdEnum, None]): Chain identifier. Only `CT_501` (Solana) is accepted; other values return `CHAIN_NOT_SUPPORTED` (40411).
                    amount (Union[str, None]): Sell-token amount in the token's smallest unit (positive integer string, no decimals).
                    from_token_address (Union[str, None]): Sell-token mint address (Solana Base58, case-sensitive).
                    to_token_address (Union[str, None]): Buy-token mint address (Solana Base58, case-sensitive).
                    slippage_percent (Union[str, None]): Maximum slippage as a percentage. Solana range 0 to less than 100. "0.5" means 0.5%.
                    user_wallet_address (Union[str, None]): User wallet address (Solana Base58); becomes the v0 transaction's `feePayer`.
                    quote_id (Union[str, None]): quoteId returned from `/quote` for the route to execute. TTL ~30s; expired entries return `QUOTE_EXPIRED` (40401).
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    price_impact_protection_percent (Optional[str] = None): Maximum allowed price impact percentage (0–100). Defaults to 90; set to 100 to disable.
                    auto_slippage (Optional[BuildSolanaSwapInstructionsAutoSlippageEnum] = None): When "true", slippage is auto-derived from market data and overrides `slippagePercent`. Defaults to false.
                    max_auto_slippage_percent (Optional[str] = None): Cap on auto-derived slippage (only applies when `autoSlippage=true`).
                    compute_unit_limit (Optional[str] = None): Maximum compute units the transaction may consume (analogous to EVM gasLimit). Defaults to the platform value when omitted.
                    compute_unit_price (Optional[str] = None): Priority fee per compute unit (micro-lamports). When omitted, the platform computes a value either from the `gasLevel` tier or from chain-side defaults.
                    gas_level (Optional[BuildSolanaSwapInstructionsGasLevelEnum] = None): Priority-fee tier; consulted only when `computeUnitPrice` is omitted. Defaults to "average".
                    tips (Optional[str] = None): Jito tips in SOL for MEV protection. Valid range [0.000000001, 2] (minimum 1 lamport). When specified, it is recommended to set `computeUnitPrice=0`. The platform picks one of Jito's tip accounts at random per request.
                    fee_percent (Optional[str] = None): Custom fee (referral fee / Add Fee) percentage as a decimal string. Must be paired with exactly one of `fromTokenReferrerWalletAddress` or `toTokenReferrerWalletAddress` (the two referrer addresses are mutually exclusive). Same semantics as `/swap`.

        **Range (Solana):** `(0, 10]` — greater than 0, up to 10 inclusive, max 2 decimal places. `"1.5"` means 1.5%. Values exceeding 2 decimal places are rejected with `INVALID_FEE_PERCENT` (40466).

        **`four.meme` tokens are not supported** — do not pass fee parameters when either side of the pair is a `four.meme` token.
                    from_token_referrer_wallet_address (Optional[str] = None): Wallet address that receives the fee deducted from the sell token (`FROM_TOKEN` direction). Mutually exclusive with `toTokenReferrerWalletAddress` — providing both returns `CONFLICT_REFERRER_PARAMS` (40468). Solana requires a Base58 pubkey; an invalid format returns `INVALID_REFERRER_ADDRESS` (40467). Must be paired with `feePercent`.
                    to_token_referrer_wallet_address (Optional[str] = None): Wallet address that receives the fee deducted from the buy-token output (`TO_TOKEN` direction). Mutually exclusive with `fromTokenReferrerWalletAddress` — providing both returns `CONFLICT_REFERRER_PARAMS` (40468). Solana requires a Base58 pubkey; an invalid format returns `INVALID_REFERRER_ADDRESS` (40467). Must be paired with `feePercent`. The referrer must already be activated (funded with some SOL) or the request returns `REFERRER_NOT_ACTIVATED` (40469).

                Returns:
                    ApiResponse[BuildSolanaSwapInstructionsResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.build_solana_swap_instructions(
            binance_chain_id,
            amount,
            from_token_address,
            to_token_address,
            slippage_percent,
            user_wallet_address,
            quote_id,
            recv_window,
            nonce,
            price_impact_protection_percent,
            auto_slippage,
            max_auto_slippage_percent,
            compute_unit_limit,
            compute_unit_price,
            gas_level,
            tips,
            fee_percent,
            from_token_referrer_wallet_address,
            to_token_referrer_wallet_address,
        )

    def build_swap_transaction(
        self,
        binance_chain_id: Union[str, None],
        amount: Union[str, None],
        from_token_address: Union[str, None],
        to_token_address: Union[str, None],
        user_wallet_address: Union[str, None],
        quote_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        slippage_percent: Optional[str] = None,
        approve_transaction: Optional[
            BuildSwapTransactionApproveTransactionEnum
        ] = None,
        approve_amount: Optional[str] = None,
        gas_limit: Optional[str] = None,
        gas_level: Optional[BuildSwapTransactionGasLevelEnum] = None,
        price_impact_protection_percent: Optional[str] = None,
        auto_slippage: Optional[BuildSwapTransactionAutoSlippageEnum] = None,
        max_auto_slippage_percent: Optional[str] = None,
        compute_unit_limit: Optional[str] = None,
        compute_unit_price: Optional[str] = None,
        tips: Optional[str] = None,
        fee_percent: Optional[str] = None,
        from_token_referrer_wallet_address: Optional[str] = None,
        to_token_referrer_wallet_address: Optional[str] = None,
    ) -> ApiResponse[BuildSwapTransactionResponse]:
        """
                Build Swap Transaction

                Build the on-chain swap calldata for a previously quoted route. The request is matched against the cached quote by `quoteId` (TTL ~30s); if the entry has expired, `QUOTE_EXPIRED` (40401) is returned, and if the request parameters disagree with the cached quote, `SWAP_QUOTE_MISMATCH` (40462) is returned.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "56"=BSC, "1"=Ethereum, "CT_501"=Solana).
                    amount (Union[str, None]): Sell-token amount in the token's smallest unit (positive integer string, no decimals).
                    from_token_address (Union[str, None]): Sell-token contract address.
                    to_token_address (Union[str, None]): Buy-token contract address.
                    user_wallet_address (Union[str, None]): User wallet address (transaction sender).
                    quote_id (Union[str, None]): quoteId returned from `/quote` for the route to execute. TTL ~30s; expired entries return `QUOTE_EXPIRED` (40401).
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    slippage_percent (Optional[str] = None): Maximum slippage tolerance as a percentage string. Required unless `autoSlippage=true`.

        **Range by chain:**
        - EVM chains (BSC, Ethereum, Base, etc.): `0` to `100` (inclusive)
        - Solana (`CT_501`): `0` to less than `100` (i.e. `< 100`)

        **Range by vendor:**
        - 1inch, PancakeSwap: `0` to `50` (values above 50 are rejected)
        - LiFi, LiquidMesh: `0` to `100` (EVM) or `0` to `< 100` (Solana)
        - Jupiter (Solana): `0` to less than `100`; the value is converted to basis points (`slippageBps = ceil(slippagePercent × 100)`) and applied to the on-chain swap

        `"0.5"` means 0.5% maximum slippage. When `autoSlippage=true` this field is overridden by the auto-computed value.
                    approve_transaction (Optional[BuildSwapTransactionApproveTransactionEnum] = None): When "true", `signatureData` includes the spender address and approve calldata so the client can submit it before the swap. Defaults to false.
                    approve_amount (Optional[str] = None): Override approve amount (smallest unit, positive integer string). Defaults to the swap amount.
                    gas_limit (Optional[str] = None): Gas limit override (positive integer string). EVM only.
                    gas_level (Optional[BuildSwapTransactionGasLevelEnum] = None): Gas price tier. Defaults to "average".
                    price_impact_protection_percent (Optional[str] = None): Maximum allowed price impact percentage (0–100). Defaults to 90; set to 100 to disable.
                    auto_slippage (Optional[BuildSwapTransactionAutoSlippageEnum] = None): When `"true"`, slippage is auto-derived from market data and overrides `slippagePercent`. Either `slippagePercent` or `autoSlippage=true` must be provided — omitting both returns a parameter error. Defaults to `"false"`.
                    max_auto_slippage_percent (Optional[str] = None): Cap on auto-derived slippage (only applies when `autoSlippage=true`).
                    compute_unit_limit (Optional[str] = None): Solana only — maximum compute units the transaction may consume (analogous to EVM gasLimit). Applies only when `binanceChainId=CT_501`.
                    compute_unit_price (Optional[str] = None): Solana only — priority fee per compute unit (micro-lamports), analogous to EVM gasPrice. When omitted, the platform computes a value dynamically. Applies only when `binanceChainId=CT_501`.
                    tips (Optional[str] = None): Solana only — Jito tips in SOL for MEV protection. Valid range [0.000000001, 2] (minimum 1 lamport). When specified, it is recommended to set `computeUnitPrice=0`. Applies only when `binanceChainId=CT_501`.
                    fee_percent (Optional[str] = None): Custom fee (referral fee / Add Fee) percentage as a decimal string. Must be paired with exactly one of `fromTokenReferrerWalletAddress` or `toTokenReferrerWalletAddress` (the two referrer addresses are mutually exclusive).

        **Range by chain:** `(0, 5]` for EVM chains (BSC, Ethereum, Base, etc.) and `(0, 10]` for Solana (`CT_501`) — greater than 0, up to the chain-specific maximum inclusive, max 2 decimal places. `"1.5"` means 1.5%. Values exceeding 2 decimal places are rejected with `INVALID_FEE_PERCENT` (40466).

        **`four.meme` tokens are not supported** — do not pass fee parameters when either side of the pair is a `four.meme` token.
                    from_token_referrer_wallet_address (Optional[str] = None): Wallet address that receives the fee deducted from the sell token (`FROM_TOKEN` direction). Mutually exclusive with `toTokenReferrerWalletAddress` — providing both returns `CONFLICT_REFERRER_PARAMS` (40468). Address format depends on the chain: EVM chains require `0x` + 40 hex chars; Solana (`CT_501`) requires a Base58 pubkey. An invalid format returns `INVALID_REFERRER_ADDRESS` (40467). Must be paired with `feePercent`.
                    to_token_referrer_wallet_address (Optional[str] = None): Wallet address that receives the fee deducted from the buy-token output (`TO_TOKEN` direction). Mutually exclusive with `fromTokenReferrerWalletAddress` — providing both returns `CONFLICT_REFERRER_PARAMS` (40468). Address format depends on the chain: EVM chains require `0x` + 40 hex chars; Solana (`CT_501`) requires a Base58 pubkey. An invalid format returns `INVALID_REFERRER_ADDRESS` (40467). Must be paired with `feePercent`. On Solana, the referrer must already be activated (funded with some SOL) or the request returns `REFERRER_NOT_ACTIVATED` (40469).

                Returns:
                    ApiResponse[BuildSwapTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.build_swap_transaction(
            binance_chain_id,
            amount,
            from_token_address,
            to_token_address,
            user_wallet_address,
            quote_id,
            recv_window,
            nonce,
            slippage_percent,
            approve_transaction,
            approve_amount,
            gas_limit,
            gas_level,
            price_impact_protection_percent,
            auto_slippage,
            max_auto_slippage_percent,
            compute_unit_limit,
            compute_unit_price,
            tips,
            fee_percent,
            from_token_referrer_wallet_address,
            to_token_referrer_wallet_address,
        )

    def get_aggregated_quote(
        self,
        binance_chain_id: Union[str, None],
        amount: Union[str, None],
        from_token_address: Union[str, None],
        to_token_address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        vendor: Optional[GetAggregatedQuoteVendorEnum] = None,
        user_wallet_address: Optional[str] = None,
        fee_percent: Optional[str] = None,
        fee_source: Optional[GetAggregatedQuoteFeeSourceEnum] = None,
    ) -> ApiResponse[GetAggregatedQuoteResponse]:
        """
                Get Aggregated Quote

                Query multiple DEX vendors in parallel and return the priced routes sorted by `toTokenAmount` (descending). Each route carries an independent `quoteId` (TTL ~30s) that the swap endpoint consumes to construct calldata for the chosen route.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "56"=BSC, "1"=Ethereum, "CT_501"=Solana).
                    amount (Union[str, None]): Sell-token amount in the token's smallest unit (positive integer string, no decimals). Example "1000000" = 1 USDT (decimals=6).
                    from_token_address (Union[str, None]): Sell-token contract address. EVM chains require 0x + 40 hex chars; non-EVM chains use the chain's native address format.
                    to_token_address (Union[str, None]): Buy-token contract address. Must differ from `fromTokenAddress`.
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    vendor (Optional[GetAggregatedQuoteVendorEnum] = None): Optional vendor selector. When provided, only the specified vendor is queried through the single-vendor fast path; the request bypasses the multi-vendor dual-window, early-return, and price-check logic. Values are case-sensitive and must be one of `LiquidMesh`, `Pancake`, or `Jupiter`. The vendor must also support the requested chain. An unsupported value or unavailable vendor/chain returns `PARAM_ERROR` (40001). When omitted, the API queries all applicable vendors in parallel and returns the aggregated routes.
                    user_wallet_address (Optional[str] = None): User wallet address. Required when quoting RFQ routes (equity / RWA tokens such as Ondo and BStock). This address is used as the receiver in the RFQ order and must match the wallet that signs `rfq.typedDataToSign` in the subsequent `/swap` call.
                    fee_percent (Optional[str] = None): Custom fee (referral fee / Add Fee) percentage as a decimal string. Must be paired with `feeSource` — either both present or both absent.

        **Range by chain:** `(0, 5]` for EVM chains (BSC, Ethereum, Base, etc.) and `(0, 10]` for Solana (`CT_501`) — greater than 0, up to the chain-specific maximum inclusive, max 2 decimal places. `"1.5"` means 1.5%. Values exceeding 2 decimal places are rejected with `INVALID_FEE_PERCENT` (40466).

        **`four.meme` tokens are not supported** — do not pass fee parameters when either side of the pair is a `four.meme` token.
                    fee_source (Optional[GetAggregatedQuoteFeeSourceEnum] = None): Fee deduction direction. `FROM_TOKEN` = deduct the fee from the sell token (the amount passed to the DEX is reduced to a net amount); `TO_TOKEN` = deduct the fee from the buy-token output (the user's actual received amount is reduced). Must be paired with `feePercent`.

                Returns:
                    ApiResponse[GetAggregatedQuoteResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.get_aggregated_quote(
            binance_chain_id,
            amount,
            from_token_address,
            to_token_address,
            recv_window,
            nonce,
            vendor,
            user_wallet_address,
            fee_percent,
            fee_source,
        )

    def get_aggregator_supported_chains(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        binance_chain_id: Optional[str] = None,
    ) -> ApiResponse[GetAggregatorSupportedChainsResponse]:
        """
                Get Aggregator Supported Chains

                Return blockchain networks supported by the DEX aggregator. The supported list is dynamically configured server-side and may change over time.
        Pass `binanceChainId` to filter to a single chain; omit it to get the full list.

                Args:
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    binance_chain_id (Optional[str] = None): Optional chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana). When set, only that chain is returned.

                Returns:
                    ApiResponse[GetAggregatorSupportedChainsResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.get_aggregator_supported_chains(
            recv_window, nonce, binance_chain_id
        )

    def get_erc20_approve_transaction(
        self,
        binance_chain_id: Union[str, None],
        token_contract_address: Union[str, None],
        approve_amount: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        vendor: Optional[str] = None,
    ) -> ApiResponse[GetErc20ApproveTransactionResponse]:
        """
                Get ERC-20 Approve Transaction

                Build the on-chain transaction data needed to approve the DEX router to spend a user's ERC-20 token before a swap. Calldata is encoded per the ERC-20 ABI standard (`approve()` selector + spender + amount).

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC).
                    token_contract_address (Union[str, None]): ERC-20 token contract address to approve (0x + 40 hex chars).
                    approve_amount (Union[str, None]): Approval amount in the token's smallest unit (positive integer string). Example "1000000" = 1 USDT (decimals=6).
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    vendor (Optional[str] = None): RFQ vendor name. **Required for equity / RWA tokens (Ondo, BStock)**; pass the `vendorName` from the `/quote` response (e.g. `InchFusion`, `CowSwap`, `PcsXRfq`). When provided, the backend returns approve calldata targeting the vendor-specific spender contract (e.g. 1inch Router, PcsX Permit2, CowSwap VaultRelayer) instead of the default DEX router.

        For regular (non-RWA) tokens, this parameter is optional. If omitted, the backend uses the standard DEX router. If a valid RFQ vendor is passed, the backend resolves that vendor's spender — used when buying Ondo/BStock with a stablecoin, where the from-token (e.g. USDT) itself is not an RFQ token but must be approved to the RFQ vendor's router.

                Returns:
                    ApiResponse[GetErc20ApproveTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.get_erc20_approve_transaction(
            binance_chain_id,
            token_contract_address,
            approve_amount,
            recv_window,
            nonce,
            vendor,
        )

    def get_rfq_order_status(
        self,
        order_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetRfqOrderStatusResponse]:
        """
        Get RFQ Order Status

        Query the settlement status of an RFQ order by its platform `orderId` (returned by `POST /order/submit`). Poll this endpoint until `status` reaches a terminal state: `FILLED` (settled on-chain) or `FAILED` (settlement failed).

        Args:
            order_id (Union[str, None]): Platform order ID returned by `POST /order/submit`.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetRfqOrderStatusResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.get_rfq_order_status(order_id, recv_window, nonce)

    def get_transaction_status(
        self,
        binance_chain_id: Union[str, None],
        tx_hash: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTransactionStatusResponse]:
        """
                Get Transaction Status

                Look up the on-chain status of a DEX swap by `binanceChainId` + `txHash`.
        Response semantics:
        - Transaction not found: `data` is `null` (not HTTP 404).
        - Transaction failed: `status=failed` with `errorMsg`; aggregator
          business fields (`txType`, `dexRouter`, `fromTokenDetails`,
          `toTokenDetails`) are `null`.

        - Transaction succeeded: `status=success` with full token details
          when an aggregator event is present.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "56"=BSC, "1"=Ethereum, "CT_501"=Solana).
                    tx_hash (Union[str, None]): On-chain transaction hash.
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

                Returns:
                    ApiResponse[GetTransactionStatusResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.get_transaction_status(
            binance_chain_id, tx_hash, recv_window, nonce
        )

    def quote_and_build_swap_transaction(
        self,
        binance_chain_id: Union[str, None],
        amount: Union[str, None],
        from_token_address: Union[str, None],
        to_token_address: Union[str, None],
        user_wallet_address: Union[str, None],
        vendor: Union[QuoteAndBuildSwapTransactionVendorEnum, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        slippage_percent: Optional[str] = None,
        approve_transaction: Optional[
            QuoteAndBuildSwapTransactionApproveTransactionEnum
        ] = None,
        approve_amount: Optional[str] = None,
        gas_limit: Optional[str] = None,
        gas_level: Optional[QuoteAndBuildSwapTransactionGasLevelEnum] = None,
        price_impact_protection_percent: Optional[str] = None,
        auto_slippage: Optional[QuoteAndBuildSwapTransactionAutoSlippageEnum] = None,
        max_auto_slippage_percent: Optional[str] = None,
        compute_unit_limit: Optional[str] = None,
        compute_unit_price: Optional[str] = None,
        tips: Optional[str] = None,
        fee_percent: Optional[str] = None,
        from_token_referrer_wallet_address: Optional[str] = None,
        to_token_referrer_wallet_address: Optional[str] = None,
    ) -> ApiResponse[QuoteAndBuildSwapTransactionResponse]:
        """
                Quote and Build Swap Transaction (Flash API)

                Combines quoting and swap-transaction construction into a single call. Unlike the two-step `/quote` + `/swap` flow, this endpoint does not require a prior `/quote` call or a `quoteId` — it returns the executable calldata / swapTransaction directly, eliminating one HTTP round-trip.
        Use this endpoint for latency-sensitive trading when the vendor is known upfront. The response shape is identical to `/swap`, so clients can reuse the same response parsing logic for both endpoints.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "56"=BSC, "1"=Ethereum, "CT_501"=Solana).
                    amount (Union[str, None]): Sell-token amount in the token's smallest unit (positive integer string, no decimals).
                    from_token_address (Union[str, None]): Sell-token contract address.
                    to_token_address (Union[str, None]): Buy-token contract address.
                    user_wallet_address (Union[str, None]): User wallet address (transaction sender). For Solana, a Base58 system-account address with sufficient SOL for gas.
                    vendor (Union[QuoteAndBuildSwapTransactionVendorEnum, None]): Swap vendor to use for this request. **Required** — case-sensitive; must match one of the enum values. Any other value returns `PARAM_ERROR` (40001). For multi-vendor aggregation, use `/swap` with a `quoteId` instead.
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    slippage_percent (Optional[str] = None): Maximum slippage tolerance as a percentage string. Required unless `autoSlippage=true`.

        **Range by chain:**
        - EVM chains (BSC, Ethereum, Base, etc.): `0` to `100` (inclusive)
        - Solana (`CT_501`): `0` to less than `100` (i.e. `< 100`)

        `"0.5"` means 0.5% maximum slippage. When `autoSlippage=true` this field is overridden by the auto-computed value.
                    approve_transaction (Optional[QuoteAndBuildSwapTransactionApproveTransactionEnum] = None): When "true", `signatureData` includes the spender address and approve calldata so the client can submit it before the swap. Defaults to false.
                    approve_amount (Optional[str] = None): Override approve amount (smallest unit, positive integer string). Defaults to the swap amount.
                    gas_limit (Optional[str] = None): Gas limit override (positive integer string). EVM only.
                    gas_level (Optional[QuoteAndBuildSwapTransactionGasLevelEnum] = None): Gas price tier. Defaults to "average".
                    price_impact_protection_percent (Optional[str] = None): Maximum allowed price impact percentage (0–100). Defaults to 90; set to 100 to disable.
                    auto_slippage (Optional[QuoteAndBuildSwapTransactionAutoSlippageEnum] = None): When `"true"`, slippage is auto-derived from market data and overrides `slippagePercent`. Either `slippagePercent` or `autoSlippage=true` must be provided — omitting both returns a parameter error. Defaults to `"false"`.
                    max_auto_slippage_percent (Optional[str] = None): Cap on auto-derived slippage (only applies when `autoSlippage=true`).
                    compute_unit_limit (Optional[str] = None): Solana only — maximum compute units the transaction may consume (analogous to EVM gasLimit). Applies only when `binanceChainId=CT_501`.
                    compute_unit_price (Optional[str] = None): Solana only — priority fee per compute unit (micro-lamports), analogous to EVM gasPrice. When omitted, the platform computes a value dynamically. Applies only when `binanceChainId=CT_501`.
                    tips (Optional[str] = None): Solana only — Jito tips in SOL for MEV protection. Valid range [0.000000001, 2] (minimum 1 lamport). When specified, it is recommended to set `computeUnitPrice=0`. Applies only when `binanceChainId=CT_501`.
                    fee_percent (Optional[str] = None): Custom fee (referral fee / Add Fee) percentage as a decimal string. Must be paired with exactly one of `fromTokenReferrerWalletAddress` or `toTokenReferrerWalletAddress` (the two referrer addresses are mutually exclusive).

        **Range by chain:** `(0, 5]` for EVM chains (BSC, Ethereum, Base, etc.) and `(0, 10]` for Solana (`CT_501`) — greater than 0, up to the chain-specific maximum inclusive, max 2 decimal places. `"1.5"` means 1.5%. Values exceeding 2 decimal places are rejected with `INVALID_FEE_PERCENT` (40466).

        **`four.meme` tokens are not supported** — do not pass fee parameters when either side of the pair is a `four.meme` token.
                    from_token_referrer_wallet_address (Optional[str] = None): Wallet address that receives the fee deducted from the sell token (`FROM_TOKEN` direction). Mutually exclusive with `toTokenReferrerWalletAddress` — providing both returns `CONFLICT_REFERRER_PARAMS` (40468). Address format depends on the chain: EVM chains require `0x` + 40 hex chars; Solana (`CT_501`) requires a Base58 pubkey. An invalid format returns `INVALID_REFERRER_ADDRESS` (40467). Must be paired with `feePercent`.
                    to_token_referrer_wallet_address (Optional[str] = None): Wallet address that receives the fee deducted from the buy-token output (`TO_TOKEN` direction). Mutually exclusive with `fromTokenReferrerWalletAddress` — providing both returns `CONFLICT_REFERRER_PARAMS` (40468). Address format depends on the chain: EVM chains require `0x` + 40 hex chars; Solana (`CT_501`) requires a Base58 pubkey. An invalid format returns `INVALID_REFERRER_ADDRESS` (40467). Must be paired with `feePercent`. On Solana, the referrer must already be activated (funded with some SOL) or the request returns `REFERRER_NOT_ACTIVATED` (40469).

                Returns:
                    ApiResponse[QuoteAndBuildSwapTransactionResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.quote_and_build_swap_transaction(
            binance_chain_id,
            amount,
            from_token_address,
            to_token_address,
            user_wallet_address,
            vendor,
            recv_window,
            nonce,
            slippage_percent,
            approve_transaction,
            approve_amount,
            gas_limit,
            gas_level,
            price_impact_protection_percent,
            auto_slippage,
            max_auto_slippage_percent,
            compute_unit_limit,
            compute_unit_price,
            tips,
            fee_percent,
            from_token_referrer_wallet_address,
            to_token_referrer_wallet_address,
        )

    def submit_rfq_order(
        self,
        request_id: Union[str, None],
        user_signature: Union[str, None],
        vendor: Union[SubmitRfqOrderVendorEnum, None],
        quote_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        signing_scheme: Optional[str] = None,
    ) -> ApiResponse[SubmitRfqOrderResponse]:
        """
                Submit RFQ Order

                Submit a signed RFQ order to the backend for on-chain settlement via the corresponding vendor relayer. Only used when `executionMode=RFQ` (equity / RWA tokens such as Ondo and BStock).

        **Flow**: `GET /quote` → pick an RFQ route → `GET /swap` → sign `rfq.typedDataToSign` with EIP-712 (`eth_signTypedData_v4`) → call this endpoint → poll `GET /order/{orderId}` until `FILLED` or `FAILED`.

        **Idempotency**: Submitting with the same `requestId` within 30 minutes returns the original result without re-calling the vendor. Use a new UUID for each distinct order; reuse the same UUID when retrying.

                Args:
                    request_id (Union[str, None]): Idempotency key (UUID v4). Within 30 minutes, re-submitting with the same `requestId` returns the original result without re-calling the vendor. Generate a new UUID for each new order; reuse the same UUID when retrying the same attempt.
                    user_signature (Union[str, None]): EIP-712 signature of `rfq.typedDataToSign` from the `/swap` response. Must be a hex string with `0x` prefix (132 chars = `0x` + 65 bytes). The signing wallet must match `userWalletAddress` used in `/quote`. The backend automatically corrects Ethereum `v` values `00`/`01` to `1b`/`1c`.
                    vendor (Union[SubmitRfqOrderVendorEnum, None]): RFQ vendor name — must match `rfq.vendor` from the `/swap` response. The backend validates this against `quoteId` to prevent misrouted submissions.
                    quote_id (Union[str, None]): `rfq.orderId` from the `/swap` response. The backend uses this to look up the vendor and chain context required to forward the signed order.
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    signing_scheme (Optional[str] = None): Signing scheme from `rfq.signingScheme` in the `/swap` response. Optional — when omitted the backend infers the default per vendor.

                Returns:
                    ApiResponse[SubmitRfqOrderResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._tradingApi.submit_rfq_order(
            request_id,
            user_signature,
            vendor,
            quote_id,
            recv_window,
            nonce,
            signing_scheme,
        )

    def broadcast_transactions(
        self,
        binance_chain_id: Union[str, None],
        signed_transaction: Union[str, None],
        address: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        enable_mev_protection: Optional[bool] = None,
    ) -> ApiResponse[BroadcastTransactionsResponse]:
        """
                Broadcast Transactions

                Broadcast a client-signed transaction to the chain via the Binance Web3 API relay. Returns the transaction hash and an internal `orderId` you can use to track on-chain status via the post-transaction service.
        Optional MEV protection (EVM chains only) routes the transaction through a private mempool to mitigate front-running and sandwich attacks. Tron and Solana do not support MEV protection; the flag is ignored on these chains.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana, "CT_195"=Tron).
                    signed_transaction (Union[str, None]): Client-signed raw transaction. Format depends on `binanceChainId`:  - **EVM chains**: hex-encoded RLP transaction (e.g. `0xf86c...`). - **Solana (`CT_501`)**: base64-encoded signed transaction. - **Tron (`CT_195`)**: JSON string of the signed transaction   returned by a Tron signer, containing `raw_data` (with   `contract[].parameter.value.owner_address`) and a `signature`   array. Example:   `{"raw_data":{"contract":[{"type":"TriggerSmartContract","parameter":{"value":{"owner_address":"T...","contract_address":"T...","data":"<calldata hex>","call_value":0}}}]},"signature":["..."]}`.   Note for Tron: `signedTransaction` is NOT the calldata hex passed to `simulate` / `getGasLimit` under `tronTx.triggerSmartContractParams.data`. The signer is extracted from `raw_data.contract[0].parameter.value.owner_address` and must match `address`; passing calldata hex here will be rejected with `code=40001, invalid signedTransaction: failed to parse Tron signedTransaction: syntax error`.
                    address (Union[str, None]): Sender wallet address. Used for status attribution and signature verification. On Tron and Solana the address is case-sensitive base58check.
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
                    enable_mev_protection (Optional[bool] = None): When true, route the transaction through a private mempool for MEV protection.

                Returns:
                    ApiResponse[BroadcastTransactionsResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._transactionApi.broadcast_transactions(
            binance_chain_id,
            signed_transaction,
            address,
            recv_window,
            nonce,
            enable_mev_protection,
        )

    def get_broadcast_orders(
        self,
        address: Union[str, None],
        binance_chain_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        tx_status: Optional[str] = None,
        order_id: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
    ) -> ApiResponse[GetBroadcastOrdersResponse]:
        """
        Get Broadcast Orders

        Look up broadcast orders previously submitted via `/pre-transaction/broadcast-transaction`. Filter by `txStatus` or `orderId`, paginate with `cursor`.

        Args:
            address (Union[str, None]): Sender wallet address whose orders to list.
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            tx_status (Optional[str] = None): Optional filter on transaction status (server-defined string, e.g. "pending", "success", "fail").
            order_id (Optional[str] = None): Optional filter to fetch a single order by its internal order ID.
            cursor (Optional[str] = None): Pagination cursor returned by the previous page. Omit on the first request.
            limit (Optional[int] = None): Page size. Defaults to 20.

        Returns:
            ApiResponse[GetBroadcastOrdersResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._transactionApi.get_broadcast_orders(
            address,
            binance_chain_id,
            recv_window,
            nonce,
            tx_status,
            order_id,
            cursor,
            limit,
        )

    def get_gas_limit(
        self,
        binance_chain_id: Union[str, None],
        evm_tx: Union[GetGasLimitRequestEvmTx, None],
        sol_tx: Union[GetGasLimitRequestSolTx, None],
        tron_tx: Union[GetGasLimitRequestTronTx, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetGasLimitResponse]:
        """
                Get Gas Limit

                Estimate the gas limit (or compute-unit ceiling on Solana) for an unsigned transaction.
        Provide either `evmTx` for EVM chains, `solTx` for Solana, or `tronTx` for Tron ("CT_195"), matching the value of `binanceChainId`.
        On Tron the response carries energy/bandwidth fields instead of a single gas limit; `gasLimit` is the fee limit (in sun) and the energy/bandwidth fields describe resource consumption and pricing.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana, "CT_195"=Tron). Provide `evmTx` for EVM chains, `solTx` for Solana, or `tronTx` for Tron — exactly one must be present. Note: `evmTx`, `solTx`, and `tronTx` are marked required in this schema for rendering purposes only; in practice supply exactly one matching `binanceChainId`.
                    evm_tx (Union[GetGasLimitRequestEvmTx, None]):
                    sol_tx (Union[GetGasLimitRequestSolTx, None]):
                    tron_tx (Union[GetGasLimitRequestTronTx, None]):
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

                Returns:
                    ApiResponse[GetGasLimitResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._transactionApi.get_gas_limit(
            binance_chain_id, evm_tx, sol_tx, tron_tx, recv_window, nonce
        )

    def get_gas_price(
        self,
        binance_chain_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetGasPriceResponse]:
        """
                Get Gas Price

                Query the current network gas price for the specified chain. The response shape varies by chain family:
        - EVM chains return both `evmLegacyGasPrice` (legacy gasPrice) and
          `eip1559GasPrice` (baseFee + priority/max fees) when EIP-1559 is supported.
        - Solana returns `solanaGasPrice` (compute-unit prices and Jito tips). - Tron ("CT_195") returns an empty `data` object because Tron has no
          on-chain gas-price concept; use the gas-limit endpoint instead.

        Fields not applicable to the chain family are returned as `null`.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana, "CT_195"=Tron).
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

                Returns:
                    ApiResponse[GetGasPriceResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._transactionApi.get_gas_price(binance_chain_id, recv_window, nonce)

    def get_latest_block_height(
        self,
        binance_chain_id: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetLatestBlockHeightResponse]:
        """
        Get Latest Block Height

        Return the latest block height that the Binance Web3 node has synced to for the specified chain. Callers can use this to monitor node sync progress for risk control and detect when the node lags behind the canonical chain head.

        Args:
            binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana, "CT_195"=Tron).
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetLatestBlockHeightResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._transactionApi.get_latest_block_height(
            binance_chain_id, recv_window, nonce
        )

    def get_transaction_supported_chains(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[GetTransactionSupportedChainsResponse]:
        """
        Get Transaction Supported Chains

        Return the blockchain networks supported by the Transaction service for gas estimation, simulation, and broadcasting. The list is dynamically configured server-side and may change over time.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

        Returns:
            ApiResponse[GetTransactionSupportedChainsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._transactionApi.get_transaction_supported_chains(recv_window, nonce)

    def simulate_transactions(
        self,
        binance_chain_id: Union[str, None],
        evm_tx: Union[SimulateTransactionsRequestEvmTx, None],
        sol_tx: Union[SimulateTransactionsRequestSolTx, None],
        tron_tx: Union[SimulateTransactionsRequestTronTx, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
    ) -> ApiResponse[SimulateTransactionsResponse]:
        """
                Simulate Transactions

                Simulate transaction execution off-chain to predict its outcome before broadcasting. The response includes the predicted execution status, balance changes per affected account/token, and ERC-20 allowance changes (EVM chains).
        Provide either `evmTx` (EVM chains), `solTx` (Solana), or `tronTx` (Tron "CT_195") matching `binanceChainId`. On Tron, `allowanceChanges` is returned as an empty array.
        Note: Metis (chainId 1088) is not supported by this endpoint.

                Args:
                    binance_chain_id (Union[str, None]): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana, "CT_195"=Tron). Provide `evmTx` for EVM chains, `solTx` for Solana, or `tronTx` for Tron — exactly one must be present. Note: `evmTx`, `solTx`, and `tronTx` are marked required in this schema for rendering purposes only; in practice supply exactly one matching `binanceChainId`.
                    evm_tx (Union[SimulateTransactionsRequestEvmTx, None]):
                    sol_tx (Union[SimulateTransactionsRequestSolTx, None]):
                    tron_tx (Union[SimulateTransactionsRequestTronTx, None]):
                    recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
                    nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.

                Returns:
                    ApiResponse[SimulateTransactionsResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return self._transactionApi.simulate_transactions(
            binance_chain_id, evm_tx, sol_tx, tron_tx, recv_window, nonce
        )

    def get_all_token_balances_by_address(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        address: Optional[str] = None,
        chains: Optional[str] = None,
        exclude_risk_token: Optional[bool] = None,
        page: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse[GetAllTokenBalancesByAddressResponse]:
        """
        Get All Token Balances by Address

        Return all token balances held by an address on a single chain, with pagination support. Set `excludeRiskToken=true` to filter out airdrop-risk and honeypot tokens (honeypot detection currently applies only to ETH / BSC / SOL / BASE).

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            address (Optional[str] = None): Wallet address to query.
            chains (Optional[str] = None): Currently only a single chain is supported. Passing multiple comma-separated chain IDs returns an error; multi-chain support may be added in the future.
            exclude_risk_token (Optional[bool] = None): Whether to exclude risk-flagged tokens.
            page (Optional[int] = None): Page number. Defaults to 1.
            page_size (Optional[int] = None): Page size. Range 1–100. Defaults to 20.

        Returns:
            ApiResponse[GetAllTokenBalancesByAddressResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._walletApi.get_all_token_balances_by_address(
            recv_window, nonce, address, chains, exclude_risk_token, page, page_size
        )

    def get_token_balances_by_address(
        self,
        address: Union[str, None],
        token_contract_addresses: Union[
            List[GetTokenBalancesByAddressRequestTokenContractAddressesInner], None
        ],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        exclude_risk_token: Optional[
            GetTokenBalancesByAddressExcludeRiskTokenEnum
        ] = None,
    ) -> ApiResponse[GetTokenBalancesByAddressResponse]:
        """
        Get Token Balances by Address

        Return token balances for a specific list of (chain, contract) pairs. Up to 20 entries per request. Set `excludeRiskToken="0"` to exclude risk-flagged tokens (default), or `"1"` to include them.

        Args:
            address (Union[str, None]): Wallet address to query.
            token_contract_addresses (Union[List[GetTokenBalancesByAddressRequestTokenContractAddressesInner], None]): Tokens to query, up to 20 entries.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            exclude_risk_token (Optional[GetTokenBalancesByAddressExcludeRiskTokenEnum] = None): Risk-token filter. "0"=exclude (default), "1"=include. Honeypot detection currently applies only to ETH / BSC / SOL / BASE.

        Returns:
            ApiResponse[GetTokenBalancesByAddressResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._walletApi.get_token_balances_by_address(
            address, token_contract_addresses, recv_window, nonce, exclude_risk_token
        )

    def get_transaction_detail_by_hash(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        binance_chain_id: Optional[str] = None,
        tx_hash: Optional[str] = None,
        itype: Optional[str] = None,
    ) -> ApiResponse[GetTransactionDetailByHashResponse]:
        """
        Get Transaction Detail by Hash

        Look up the full on-chain transaction detail by `binanceChainId` + `txHash`. Returns one or more entries describing transaction inputs, outputs, internal calls, and token transfers.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            binance_chain_id (Optional[str] = None): Unique chain identifier (e.g. "1"=Ethereum, "56"=BSC, "CT_501"=Solana).
            tx_hash (Optional[str] = None): On-chain transaction hash.
            itype (Optional[str] = None): Transaction-type filter (UTXO chains). Reserved — currently has no effect. Values: "0"=outer native-token, "1"=internal contract native-token, "2"=token transfer.

        Returns:
            ApiResponse[GetTransactionDetailByHashResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._walletApi.get_transaction_detail_by_hash(
            recv_window, nonce, binance_chain_id, tx_hash, itype
        )

    def get_transactions_by_address(
        self,
        address: Union[str, None],
        chains: Union[str, None],
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        token_contract_address: Optional[str] = None,
        begin: Optional[int] = None,
        end: Optional[int] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = None,
    ) -> ApiResponse[GetTransactionsByAddressResponse]:
        """
        Get Transactions by Address

        Return on-chain transaction history for a wallet address across one or more chains. Results are limited to the most recent 6 months and sorted by time (descending). Supports cursor pagination, time-range filtering, and token-contract filtering.

        Args:
            address (Union[str, None]): Wallet address to query.
            chains (Union[str, None]): Comma-separated list of chain identifiers.
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            token_contract_address (Optional[str] = None): Optional token contract address filter.
            begin (Optional[int] = None): Optional start timestamp (Unix milliseconds, inclusive).
            end (Optional[int] = None): Optional end timestamp (Unix milliseconds, inclusive).
            cursor (Optional[str] = None): Pagination cursor returned by the previous page.
            limit (Optional[int] = None): Page size. Range 1–100. Defaults to 20.

        Returns:
            ApiResponse[GetTransactionsByAddressResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._walletApi.get_transactions_by_address(
            address,
            chains,
            recv_window,
            nonce,
            token_contract_address,
            begin,
            end,
            cursor,
            limit,
        )

    def get_wallet_supported_chains(
        self,
        recv_window: Optional[int] = None,
        nonce: Optional[str] = None,
        binance_chain_id: Optional[str] = None,
    ) -> ApiResponse[GetWalletSupportedChainsResponse]:
        """
        Get Wallet Supported Chains

        Return blockchain networks for which the Wallet service can return balances. The supported list is dynamically configured server-side and may change over time. Pass `binanceChainId` to filter to a single chain; omit to get the full list.

        Args:
            recv_window (Optional[int] = None): Allowed time deviation in milliseconds (default: 5000, max: 60000).
            nonce (Optional[str] = None): Unique request identifier for anti-replay; falls back to X-OC-SIGN if omitted.
            binance_chain_id (Optional[str] = None): Optional chain identifier. When set, only that chain is returned.

        Returns:
            ApiResponse[GetWalletSupportedChainsResponse]

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return self._walletApi.get_wallet_supported_chains(
            recv_window, nonce, binance_chain_id
        )
