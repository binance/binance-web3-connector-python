# coding: utf-8

"""
Binance Web3 Wallet REST API

Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from binance_sdk_web3_wallet.rest_api.models.defi_tx_response_data_list_inner import (
    DefiTxResponseDataListInner,
)
from binance_sdk_web3_wallet.rest_api.models.defi_tx_response_preview import (
    DefiTxResponsePreview,
)
from typing import Set
from typing_extensions import Self


class DefiTxResponse(BaseModel):
    """
    Unified response of the five build endpoints (deposit / redeem / lp-add / lp-remove / claim). `dataList` is ordered: APPROVE items come first, the main action follows — sign and broadcast each item in order. `preview` is non-null only when the request set `simulate=true`. `redeemDelayDays` is populated only by `/transaction/redeem`.
    """  # noqa: E501

    data_list: Optional[List[DefiTxResponseDataListInner]] = Field(
        default=None,
        description="Ordered unsigned transactions; APPROVE precedes the main action.",
        alias="dataList",
    )
    preview: Optional[DefiTxResponsePreview] = None
    redeem_delay_days: Optional[List[StrictStr]] = Field(
        default=None,
        description='Redeem waiting period as a `[min, max]` day pair of decimal strings. Populated **only** by `/transaction/redeem`; `null` on the other build endpoints or when unavailable. Interpretation: `[]` (empty) = instant, no waiting period; `["7","7"]` = fixed 7 days; `["7","10"]` = 7–10 days. The wait starts after the redeem transaction is confirmed on-chain.',
        alias="redeemDelayDays",
    )
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["dataList", "preview", "redeemDelayDays"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def is_array(cls) -> bool:
        return False

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DefiTxResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set(
            [
                "additional_properties",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in data_list (list)
        _items = []
        if self.data_list:
            for _item_data_list in self.data_list:
                if _item_data_list:
                    _items.append(_item_data_list.to_dict())
            _dict["dataList"] = _items
        # override the default output from pydantic by calling `to_dict()` of preview
        if self.preview:
            _dict["preview"] = self.preview.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DefiTxResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "dataList": (
                    [
                        DefiTxResponseDataListInner.from_dict(_item)
                        for _item in obj["dataList"]
                    ]
                    if obj.get("dataList") is not None
                    else None
                ),
                "preview": (
                    DefiTxResponsePreview.from_dict(obj["preview"])
                    if obj.get("preview") is not None
                    else None
                ),
                "redeemDelayDays": obj.get("redeemDelayDays"),
            }
        )
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj
