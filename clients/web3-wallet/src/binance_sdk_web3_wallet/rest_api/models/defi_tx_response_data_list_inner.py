# coding: utf-8

"""
Binance Web3 Wallet REST API

Cross-chain wallet, market, trading, transaction, and B402 payment APIs for the Binance Web3 API platform.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Set
from typing_extensions import Self


class DefiTxResponseDataListInner(BaseModel):
    """
    A single unsigned transaction to sign and broadcast. The `dataList` is ordered: APPROVE items precede the main action. EVM chains return `to`/`value` (hex) and hex calldata in `data`. **Number-format conventions for this object** — `value` is a `0x`-prefixed hex string; `gasLimit`, `gasPrice`, `maxPriorityFeePerGas`, and `maxFeePerGas` are decimal strings. Mixed hex/decimal within the same object is by design (it mirrors the on-chain transaction fields); always parse gas fields with base-10 and `value` with base-16. **BSC and EIP-1559**: this release is BSC only. BSC is an EIP-1559 chain, so build responses populate `maxFeePerGas` and `maxPriorityFeePerGas`; `gasPrice` is `null` (legacy-only). The nullable markers below are retained so the schema also describes non-1559 chains in later releases — on BSC the 1559 fields are present and the legacy `gasPrice` is `null`. `to` and `value` are non-null for every action item on BSC.
    """  # noqa: E501

    call_data_type: Optional[StrictStr] = Field(
        default=None,
        description="Action kind of this item: - `APPROVE` — ERC-20 approval, must be signed/broadcast and confirmed before the main action - `DEPOSIT` / `REDEEM` / `LP_ADD` / `LP_REMOVE` / `CLAIM` — the main action.   `LP_ADD` / `LP_REMOVE` are the LP add / remove legs (the same build path as   deposit / redeem for liquidity-pool investments; the kind reflects the on-chain action,   not the entry endpoint).",
        alias="callDataType",
    )
    var_from: Optional[StrictStr] = Field(
        default=None,
        description="User wallet address (the signer / sender). EIP-55 checksummed — normalize with `to_checksum_address` before signing if your signer requires it.",
        alias="from",
    )
    to: Optional[StrictStr] = Field(
        default=None,
        description="Target contract address. EIP-55 checksummed, same format as `from`. Non-null on BSC for every action item.",
    )
    value: Optional[StrictStr] = Field(
        default=None,
        description="Native-asset value as a `0x`-prefixed hex string. Non-null on BSC for action items.",
    )
    data: Optional[StrictStr] = Field(
        default=None,
        description="Serialized transaction data, as a `0x`-prefixed hex calldata string.",
    )
    gas_limit: Optional[StrictStr] = Field(
        default=None,
        description="Suggested gas limit (decimal string). `null` when estimation fails.",
        alias="gasLimit",
    )
    gas_price: Optional[StrictStr] = Field(
        default=None,
        description="Legacy gas price, decimal string. `null` on EIP-1559 chains such as BSC (use `maxFeePerGas` / `maxPriorityFeePerGas` instead).",
        alias="gasPrice",
    )
    max_priority_fee_per_gas: Optional[StrictStr] = Field(
        default=None,
        description="EIP-1559 priority fee in wei (decimal string). `null` on non-1559 chains.",
        alias="maxPriorityFeePerGas",
    )
    max_fee_per_gas: Optional[StrictStr] = Field(
        default=None,
        description="EIP-1559 max fee per gas in wei (decimal string). `null` on non-1559 chains.",
        alias="maxFeePerGas",
    )
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = [
        "callDataType",
        "from",
        "to",
        "value",
        "data",
        "gasLimit",
        "gasPrice",
        "maxPriorityFeePerGas",
        "maxFeePerGas",
    ]

    @field_validator("call_data_type")
    def call_data_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(
            ["APPROVE", "DEPOSIT", "REDEEM", "LP_ADD", "LP_REMOVE", "CLAIM"]
        ):
            raise ValueError(
                "must be one of enum values ('APPROVE', 'DEPOSIT', 'REDEEM', 'LP_ADD', 'LP_REMOVE', 'CLAIM')"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def is_array(cls) -> bool:
        return False

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DefiTxResponseDataListInner from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set(
            [
                "additional_properties",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if to (nullable) is None
        # and model_fields_set contains the field
        if self.to is None and "to" in self.model_fields_set:
            _dict["to"] = None

        # set to None if value (nullable) is None
        # and model_fields_set contains the field
        if self.value is None and "value" in self.model_fields_set:
            _dict["value"] = None

        # set to None if gas_limit (nullable) is None
        # and model_fields_set contains the field
        if self.gas_limit is None and "gas_limit" in self.model_fields_set:
            _dict["gasLimit"] = None

        # set to None if gas_price (nullable) is None
        # and model_fields_set contains the field
        if self.gas_price is None and "gas_price" in self.model_fields_set:
            _dict["gasPrice"] = None

        # set to None if max_priority_fee_per_gas (nullable) is None
        # and model_fields_set contains the field
        if (
            self.max_priority_fee_per_gas is None
            and "max_priority_fee_per_gas" in self.model_fields_set
        ):
            _dict["maxPriorityFeePerGas"] = None

        # set to None if max_fee_per_gas (nullable) is None
        # and model_fields_set contains the field
        if self.max_fee_per_gas is None and "max_fee_per_gas" in self.model_fields_set:
            _dict["maxFeePerGas"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DefiTxResponseDataListInner from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "callDataType": obj.get("callDataType"),
                "from": obj.get("from"),
                "to": obj.get("to"),
                "value": obj.get("value"),
                "data": obj.get("data"),
                "gasLimit": obj.get("gasLimit"),
                "gasPrice": obj.get("gasPrice"),
                "maxPriorityFeePerGas": obj.get("maxPriorityFeePerGas"),
                "maxFeePerGas": obj.get("maxFeePerGas"),
            }
        )
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj
